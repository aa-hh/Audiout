- T-COMMIT-SNAP: window-snapshot offscreen PNG renderer committed (87670b9)
- T-MIXER: Fixed MixerViewController title clipping (repinned to safeAreaLayoutGuide) and top-down row flow (flipped documentView subclass); target build blocked by pre-existing SidebarViewController.swift errors (out of scope, owned by another task) so full verify/snapshot could not run.
- T-ROW: MembershipRowView.swift authored (checkbox + icon + name, unavailable dimmed/annotated, test_* hooks); compiles clean in isolation — current AudioutedWindowUI build fails only due to a concurrent sibling task's in-progress edit to SidebarViewController.swift (missing newGroupFromSelectionTapped), unrelated to this file
- T-SIDEBAR: SidebarViewController: always-visible Groups section w/ empty-state row, labeled New Group button (imageLeading), removed right-click context menu; swift build --target AudioutedWindowUI succeeds
- T-EDITOR: GroupEditorViewController made edit-only (draft/create mode deleted, moved to sheet); membership list now uses MembershipRowView (available + unavailable-but-member candidate rule); layout capped to ~400pt column and title repinned to safeAreaLayoutGuide; MixerWindowController.swift left referencing removed draft symbols for the integration task.
- T-SHEET: Added GroupCreationSheetController.swift (new sheet: name field + available-only MembershipRowView checklist + live count + Cancel/Create); GroupCreationSheetController.swift itself compiles clean, but 'swift build --target AudioutedWindowUI' currently fails in MixerWindowController.swift (unrelated, out-of-scope file) which still references onDidCreateGroup/onDidCancelCreate/showNewDraft/isCreatingDraft removed from GroupEditorViewController.swift by a concurrent sibling task -- needs that integration task to land.
- T-WIRE: MixerWindowController + ToolbarController rewired config-only (title "Groups", selection no longer activates, creation via GroupCreationSheetController, presets popup removed); AudioutedWindowUI + AudioutedApp build GREEN
- T-DOCS: updated AudioutedWindowUI/AGENTS.md to match group-creation-window design revamp (configuration-only, sheet-based creation, MembershipRowView, edit-only editor)
- T-HARNESS: window-harness (40 checks) + window-snapshot updated to config-only Groups window (create-sheet/dedup/select!=activate/master-via-model); both targets build green, harness exits 0, snapshot writes 6 PNGs (1-default, 2-create-sheet [sheet view rendered standalone], 3-edit-group).
- T-TESTS: rewrote MixerWindowControllerTests.swift for the config-only sheet-based flow (22 new/rewritten cases); fixed a genuine loadView() infinite-recursion crash in GroupEditorViewController.swift (view.safeAreaLayoutGuide -> container.safeAreaLayoutGuide) that was blocking every editor-pane test; full suite green: 443 tests, 2 skipped, 0 failures.
- T-VERIFY: gate all green (build/tests/harnesses/snapshot); mixer default + edit-group PNGs match approved design in light+dark, but create-sheet PNG shows the membership checklist collapsed to zero height (rows not visible) and the dark-mode background does not render — real GroupCreationSheetController layout bug, not fixed per scope

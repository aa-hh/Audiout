# T3a — Popover entrance + controller-level states

**Branch:** `claude/fix-popover-entrance` (fork from current branch HEAD; working tree is clean — nothing uncommitted to carry).
**Repo root (quote it — the path has a space):** `/Users/alechenderson/Projects/AirPlay Controller/.claude/worktrees/xenodochial-ardinghelli-fa348b`
All paths below are relative to the repo root. Read `CLAUDE.md`, `AGENTS.md`, `AudioutCore/AGENTS.md`, and `AudioutCore/Sources/AudioutPopoverUI/AGENTS.md` first — their rules bind every edit here.

**BINDING TOOL RULE:** every compile and every test run goes through the wrapper scripts, which route work to the remote test mule: `bash scripts/build.sh` and `bash scripts/run-tests.sh --filter <Suite>` (full suite only for the final check). A bare swift build / swift test is FORBIDDEN — it opts out of the mule, the machine-wide concurrency cap, and the sources cache. `AUDIOUT_BUILD_LOCAL=1` only if the mule is unreachable, and report that you used it. Never pipe `run-tests.sh` through `| tail` (it eats the exit code — read the pass/fail line from the full output). Never kill or abandon an in-flight remote test run (orphaned legs pin the build lock).

**Files this track owns (edit only these, plus the test/harness files a deletion forces — listed per step):**
- `AudioutCore/Sources/AudioutPopoverUI/AppSurfaceController.swift`
- `AudioutCore/Sources/AudioutPopoverUI/SurfaceSplashView.swift` (owned; no edit ends up needed)
- `AudioutCore/Sources/AudioutPopoverUI/PopoverController.swift`
- `AudioutCore/Sources/AudioutPopoverUI/PopoverPanelViewController.swift`
- `AudioutCore/Sources/AudioutPopoverUI/SurfaceToolbar.swift`
- `AudioutCore/Sources/AudioutPopoverUI/SilenceFallbackBannerView.swift`
- `AudioutCore/Sources/AudioutPopoverUI/SystemAirPlayNoteBannerView.swift`
- `AudioutCore/Sources/AudioutPopoverUI/DiscoverySettleTracker.swift` (owned; no edit ends up needed)
- `AudioutCore/Sources/AudioutCore/GroupController.swift` (dead-code deletion sites only)
- Forced by the deletion / new tests: `AudioutCore/Sources/popover-harness/main.swift`, `AudioutCore/Tests/AudioutCoreTests/PopoverControllerTests.swift`, `.../GroupControllerTests.swift`, `.../AppSurfaceControllerTests.swift`, `.../SurfaceToolbarTests.swift`, `.../NoteBannerColorTests.swift`, `.../PopoverDeviceVisibilityTests.swift`, `dev/notes/stability-audit-2026-07-18.md` (one bullet)

**DO NOT TOUCH:** `DeviceRowView.swift`, `AppRowView.swift`, `GroupRowView.swift`, `FeedPillView.swift`, `ConnectionDiagnosisView.swift`, `MainOutRowView.swift` (T3b owns all row views), `Tokens.swift` (T9), `AppDelegate.swift` / anything in `AudioutApp/` (T7), `NativeBackend.swift` (T1), `popover-snapshot/main.swift` (its local-mix-blocked scenario retirement is handed to T3b — see Handoff). Never regenerate any checked-in snapshot PNG.

**Collision notes (two parallel tracks add small hunks to `PopoverController.swift` — leave these regions exactly as they are):**
- T1 adds a note kind in the note-precedence slot: `applyNoteSlot()` at `PopoverController.swift:1096` and the precedence comment block at `:993-1006`. Do not reorder, reformat, or edit that machinery. (Your Step 7 passes an action through `setBanner` — the SILENCE banner, `:983`/`:1472` — which is a different slot; that is fine.)
- T7 touches `statusMasterVolume` (`PopoverController.swift:1185-1188` — note it already EXISTS; T7 modifies it). Leave `:1183-1188` untouched.
- T3b will edit `DeviceRowView.swift`/`AppRowView.swift` (drag-flag root fix, cursor rects, blocked-path deletion). Nothing in this order references a symbol T3b adds — every step compiles against today's code.

**Already-fixed check:** none of this track's findings are already fixed in the tree — every anchor below was re-verified on 2026-08-27 at the cited lines.

---

## Goal

Fix the popover's first ten seconds and its controller-level dishonesty for the production release: the first menu-bar click currently shows nothing for up to ~3 s and swallows every further click (audit P0-1); an empty device card misdirects to Bluetooth with no searching/empty/permission story (P1-1); the two alarm banners never re-theme and one is a dead end (P1-7/P1-8/P2-8); a rebuild can tear the fader out from under a live drag (P1-9 controller half); Quit sits one pixel from Pin behind a speaker-off glyph (P2-4); the Bluetooth Connect row looks disabled (P2-5); ~60 lines of unreachable "blocked" machinery mislead readers (P2-7); hidden-surface events pay for diffs nobody reads (perf P1-13); a covered pinned surface burns CPU 24/7 (perf P2-12); a dated telemetry write sits in the render path (perf P2-17); and "Save Selected Devices as group" swallows failures (hardening 11). This is one of several parallel audit-fix tracks; hold exactly this scope.

## Verified facts

All checked 2026-08-27 in this worktree; the executor may rely on them without re-deriving.

- `AppSurfaceController.show(anchorRect:)` swallows repeat clicks during the first-open reveal wait: `guard !isRevealPending else { return }` — `AppSurfaceController.swift:270`. The reveal ceiling is `static let revealCeiling: TimeInterval = 3.0` at `:191`. `revealFirstOpen()` (`:328-368`) guards on `isRevealPending`, disarms the backstop timer, re-measures, fronts once, and lays the splash over settled content. `test_fireRevealCeiling()` (`:693`) calls `revealFirstOpen()` directly.
- The deferred-reveal design is an owner decision and the AGENTS-documented ONLY cure for the open jump: AppKit applies `preferredContentSize` a runloop late, so an on-screen window "cannot be 'held' at a wrong size … it can only be fronted already-correct" — `AudioutPopoverUI/AGENTS.md:31` and the comment at `AppSurfaceController.swift:341-348`. Therefore "front immediately and absorb the resize under the splash" is INFEASIBLE without new size-suppression machinery; the ceiling-cut + click-cancels-wait variant is the one to build (see Open decisions).
- Splash gating: `SurfaceSplashView.wouldPresent` (`SurfaceSplashView.swift:86-90`); hold 0.7 s / ceiling 2.7 s / fade 0.25 s at `:35,41,43`; Reduce Motion and headless skip the splash entirely (`shouldPresent`, `:78-80`). No splash-file change is needed for this order.
- Existing reveal tests (`AppSurfaceControllerTests.swift:939-1048`) assert deferral, settle-reveal, backstop, and once-per-process — none asserts that a repeat click returns early, so click-cancels-wait breaks no existing test.
- `update(devices:)` computes `expectedSubsections` / `deviceSetChanged` OUTSIDE the `isEffectivelyShown` gate and reads them only inside it — `PopoverController.swift:892-896`. The `STABILITY(D4)` mid-drag-rebuild marker sits at `:882`. The hidden branch deliberately does no rebuild (`:914-919`).
- Rows report volume drags to the controller through `deviceRow(_:didSetVolume:for:)` (`PopoverController.swift:3720`), `appRow(_:didSetVolume:for:)` (`:4345`), `mainOutRow(_:didSetMaster:)` (`:4317`). The row-side flag `isDraggingSlider` is `private` (`DeviceRowView.swift:1339`) — NOT readable from the controller; the row files are T3b's. `NSApp` must be optional-chained in this codebase (`DeviceRowView.swift:1974-1978` documents `NSApp` being nil under filtered test runs).
- Fix sketch on record for the mid-drag rebuild: "have `rebuild()` skip or defer while any row's drag flag is set" — `dev/notes/stability-audit-2026-07-18.md:122-135`.
- Device sections: `deviceSections()` (`PopoverController.swift:1513-1525`) yields This Mac / AirPlay / Cast / Bluetooth; titles at `:1490-1493` ("This Mac", "AirPlay Devices", "Bluetooth Devices", "Cast Devices"); `rendersHeader(_:)` (`:1505-1507`) hides empty sections except Bluetooth. `addSubsectionRows(_:)` (`:1592-1602`) mounts `makeBluetoothConnectRow()` when the BT section is empty. The section loop is `:1374-1397`; `renderedDeviceOrder()` at `:1532-1534`.
- The BT Connect row is a borderless `.accessoryBar` `NSButton` titled `"Connect a Bluetooth device…"`, title colored via `attributedTitle` (`:2617-2644`); `bluetoothConnectButton` is `weak var` at `:1565`; `test_fireBluetoothConnectClick()` at `:3641`; the one test driving it is `PopoverDeviceVisibilityTests.swift:253`.
- The gold ban and the not-a-pill rationale for that row are recorded at `:2608-2616` — keep both.
- `rebuildForOpen()` region ends at `:1255`; `rebuild()` starts `:1260`; the rebuild tail re-applies the banner and note slot at `:1470-1475` (`panel.setBanner(localFallbackActive ? Self.localFallbackBannerText : nil)` at `:1472`).
- `surfaceDidShow()` = `PopoverController.swift:4422-4426` (sets `hostIsShown`, installs deselect monitor, `onMeteringActiveChange?(true)`); `surfaceDidHide()` = `:4465-4494`. `isEffectivelyShown` = `:1206`. AGENTS rule: hosts announce visibility with exactly these two calls; the panel never reads a window itself (`AudioutPopoverUI/AGENTS.md:28`).
- No occlusion handling exists anywhere: grep for `occlusionState` / `didChangeOcclusionState` / `didHideNotification` across `AudioutCore/Sources` returns nothing.
- Banners: `SilenceFallbackBannerView` stamps layer colors at init (`:41-42`), overrides `updateLayer()` (`:66-70`) but NOT `wantsUpdateLayer` — the re-stamp never runs; container claims `.staticText` at `:57-58`. Same pattern in `SystemAirPlayNoteBannerView`: stamp `:120-121`, `updateLayer()` `:174-178`, `.staticText` `:154-155`. The note banner's `Action` struct is `:24-28`; its button build/layout is `:87-104,140-152`; test seams `:166-170`. The correct appearance idiom to copy is `ConnectionDiagnosisView.applyBackgroundTint` (`:225-232`, `effectiveAppearance.performAsCurrentDrawingAppearance`) and its a11y group pattern (`configureAccessibility`, `:274-278`: `setAccessibilityElement(true)` + `.group` + composed label).
- `NoteBannerColorTests.swift` constructs both banners headless and asserts `test_backgroundColor`/`test_borderColor`, including a direct `banner.updateLayer()` drive (`:76-83`).
- `setBanner(_:)` = `PopoverPanelViewController.swift:1310-1328`; `setSystemAirPlayNote(_:action:severity:)` = `:1353-1375` with seams `test_systemAirPlayNoteHasActionButton` / `test_tapSystemAirPlayNoteAction` nearby (weak typed ref kept at `:1344`).
- Retry path: `GroupController.requestReconnect(for:)` → `backend.retryOutput(id)` (`GroupController.swift:404-406`); "Try again" must be per-device re-kicks, never a broad routing re-apply (`AudioutPopoverUI/AGENTS.md:10`). `MockBackend.retryOutput` flips a still-desired `.failed` device to `.connected` (`MockBackend.swift:300-319`), so tests can observe a retry.
- `setLocalFallbackActive` = `PopoverController.swift:977-988`; banner text constant `:967`; `devicesByID` is the controller's own snapshot.
- Dead "blocked" machinery, all unreachable (producer retired at `:2215-2220`, which no longer computes `blocked`): `GroupController.localMixRefusalReason` (`GroupController.swift:262-269`), `GroupController.canSelectLocalSpeaker` (`:408-413`); in `PopoverController`: `blockedNoteByID` (`:615-619`, cleared `:1295`), `toggleBlockedNote(for:reason:)` + `makeRefusalNoteRow(text:)` (`:2648-2690` region), `deviceRowDidRequestBlockedExplanation` (`:3764-3773`), `test_isBlockedNoteShown(id:)` (`:3460-3464`, zero external callers — verified by grep). Deleting the controller's `deviceRowDidRequestBlockedExplanation` compiles: the delegate protocol has a no-op default at `DeviceRowView.swift:3101`.
- Compile-forced external references to the deleted GroupController symbols: `popover-harness/main.swift:173` (`canSelectLocalSpeaker` expectation), `PopoverControllerTests.swift:227` (same), `GroupControllerTests.swift:502,533-541,650` (`canSelectLocalSpeakerAlwaysTrue` test + comments). `popover-snapshot/main.swift` references them only in comments (`:882,907`) — no compile break.
- `presentRefusal(_:)` (`:3307-3313`) and `handleSelection` (`:3279-3305`) are LIVE (genuine model refusals) — keep them.
- Telemetry write to remove: `PopoverController.swift:2231-2240` (`cast_pending_render`), inside `applySelectionState`'s per-row path; `inActiveTarget` at `:2230` is still used by `row.apply` at `:2249` — keep it. No test references `cast_pending_render` (grep: only this site).
- "Save Selected Devices as group": `saveCurrentSetup()` at `:3187-3192` does `_ = try? controller.saveCurrentSetupAsGroup(name: name)`. `saveCurrentSetupAsGroup(name:id:)` throws (`GroupController.swift:616`). The house pattern to adapt: `GroupEditorViewController.saveOrReport` / `presentPersistFailureAlert` (`AudioutWindowUI/GroupEditorViewController.swift:681-706`) — do/catch, `test_*` flag, sheet alert only when `view.window` exists (headless-safe).
- Toolbar: default item order `[tabs, .flexibleSpace, title, .flexibleSpace, pin, quit]` (`SurfaceToolbar.swift:199-202`); Quit item uses the `power` symbol (`:297-306`); the centered lockup sets `item.label = ""` (`:283`). `SurfaceToolbarTests.attachingMaterializesTheItemsInOrder` pins the order (`SurfaceToolbarTests.swift:31-43`). Deployment floor is macOS 14 (symbols from SF Symbols 3 / macOS 12 are safe).
- Local Network permission: no queryable signal exists outside `SetupModel` (app layer, T7's to wire); the popover mentions permissions only at `:1153` (PTP copy). So the permission-denied card variant needs a host-injected provider closure (this order adds the seam; T7 wires it — see Handoff).
- Tests must stay invisible: nothing a test does may reach the screen; presentation sits behind `HeadlessRuntime.isActive` or `test_*` seams (`AudioutCore/AGENTS.md:700-730`). Menus never `popUp` headless (`PopoverController.swift:2570-2577`).
- Baseline (run 2026-08-27, this worktree, via the wrapper): `bash scripts/run-tests.sh --filter PopoverControllerTests --filter AppSurfaceControllerTests --filter SurfaceToolbarTests --filter NoteBannerColorTests --filter GroupControllerTests` → **"Test run with 262 tests in 5 suites passed after 52.459 seconds."**

## Steps

Keep diffs minimal — no drive-by cleanups, no comment rewrites beyond what a step names. Order below keeps the build green after every step.

### 1. Delete the dead local-mix "blocked" machinery (P2-7)
1a. `GroupController.swift`: delete `localMixRefusalReason` (`:262-269`) and `canSelectLocalSpeaker(_:)` (`:408-413`) including their doc comments.
1b. `PopoverController.swift`: delete `deviceRowDidRequestBlockedExplanation(_:)` (`:3764-3773`), `toggleBlockedNote(for:reason:)` and `makeRefusalNoteRow(text:)` and the `// MARK: Blocked local-mix refusal note` section around `:2648-2690`, the `blockedNoteByID` property (`:615-619`) and its `removeAll()` at `:1295`, and `test_isBlockedNoteShown(id:)` (`:3460-3464`). Keep `presentRefusal` / `handleSelection` / `test_lastRefusalReason` — they serve live model refusals. Trim the stale halves of the comment block at `:3272-3277` only where they name deleted symbols.
1c. Forced follow-ons, smallest possible edits: `popover-harness/main.swift:173` — delete that one `checks.expect(controller.canSelectLocalSpeaker(...))` expectation. `PopoverControllerTests.swift:227` — delete that one `#expect`. `GroupControllerTests.swift` — delete the `canSelectLocalSpeakerAlwaysTrue` test (`:533-541`) and the single `#expect` at `:502` (adjust its surrounding test minimally); reword a comment only if it names a deleted symbol as currently existing.
1d. Do NOT touch `DeviceRowView` or `popover-snapshot/main.swift` — see Handoff.

### 2. Remove the dated telemetry write (perf P2-17)
`PopoverController.swift:2231-2240`: delete the `// Live diagnosis (2026-08-23)…` comment and the whole `if device.isCast, castVolumePendingIDs.contains(device.id) { Telemetry.log(.cast, "cast_pending_render", …) }` block. Keep `let inActiveTarget…` at `:2230` (still consumed at `:2249`).

### 3. Hoist the structural diff inside the visibility gate (perf P1-13)
`PopoverController.swift:892-896`: move the two statements computing `expectedSubsections` and `deviceSetChanged` INSIDE the `if isEffectivelyShown {` block, immediately above the `if routesChanged || …` line. No other logic change. The comment block at `:883-891` moves with them.

### 4. Gate structural rebuilds against a live slider drag (P1-9, controller half)
The seam the controller reads: the rows already report every drag step through the three volume delegate methods (facts above); the row-side flag is private and its root fix is T3b's — build nothing that needs it.
- Add to `PopoverController`: a private `liveSliderDragUntil: CFTimeInterval = 0`, a private constant `sliderDragGraceSeconds: TimeInterval = 0.3`, and a private `noteSliderGesture()` that reads `NSApp?.currentEvent?.type` (optional-chained — see Verified facts): on `.leftMouseDragged` or `.leftMouseDown` set `liveSliderDragUntil = CACurrentMediaTime() + sliderDragGraceSeconds`; on `.leftMouseUp` set it to 0; any other/nil event leaves it unchanged. The time decay is deliberate: an Esc-cancelled drag self-heals within 0.3 s, so no stuck-flag failure mode exists on this side.
- Call `noteSliderGesture()` first in `deviceRow(_:didSetVolume:for:)` (`:3720`), `appRow(_:didSetVolume:for:)` (`:4345`), and `mainOutRow(_:didSetMaster:)` (`:4317`).
- In `update(devices:)`'s shown branch (after Step 3's hoist): when the structural condition (`routesChanged || deviceSetChanged || validTargetsChanged || mainOutMembersChanged`, or a deferral pending from a previous update) is true BUT `CACurrentMediaTime() < liveSliderDragUntil`, set a private `structuralRebuildDeferred = true` and take the existing refresh branch (`:900-912`) instead. When the gate is open and (structural condition OR `structuralRebuildDeferred`) holds, run the rebuild branch and clear the flag. Also set `structuralRebuildDeferred = false` at the top of `rebuild()` (any rebuild satisfies the debt; `rebuildForOpen()` reaches it too). A drag always produces further backend echoes, so the deferred rebuild lands within the grace window — no timer.
- Delete the `STABILITY(D4)` marker line at `:882`. In `dev/notes/stability-audit-2026-07-18.md`, edit ONLY the "Structural rebuild mid-drag detaches the tracked slider" bullet (`:122-126`) to note it is fixed (controller-side defer; the row-flag clearing sub-item stays open). Do not move D4 to Resolved — its other sub-items remain.
- Test seams: `func test_setLiveSliderDrag(_ active: Bool)` (sets `liveSliderDragUntil` to `.greatestFiniteMagnitude` or 0) and `var test_structuralRebuildDeferred: Bool`.
- New test in `PopoverControllerTests`: with the panel shown (`test_isShownOverride`), activate `test_setLiveSliderDrag(true)`, push an `update(devices:)` carrying a device-set change, assert `test_rebuildCount` did NOT increase and `test_structuralRebuildDeferred` is true; then `test_setLiveSliderDrag(false)`, push another update (even a no-change one), assert the rebuild ran and the flag cleared.

### 5. Report a failed "Save Selected Devices as group" (hardening 11)
`PopoverController.swift:3187-3192` (`saveCurrentSetup()`): replace `_ = try? …` with do/catch. On success: `rebuild()` as today. On catch: set a new `public private(set) var test_saveGroupFailureReported = false` flag to true, then present the failure exactly in `GroupEditorViewController.presentPersistFailureAlert`'s shape (`GroupEditorViewController.swift:696-706`): guard a hosting window (`panel.view.window` — skip entirely when nil, which is every headless run), `NSAlert` as a sheet, message `"Couldn’t save the group."`. Still call `rebuild()` after the catch so the card reflects the true (unsaved) state. New test: drive `saveCurrentSetup` through the "+"-menu item dispatch (`NSMenu.performActionForItem(at:)` — house rule) against a `GroupController` whose store fails, reusing whatever failure-injection the existing `GroupControllerTests` persistence tests already use; assert `test_saveGroupFailureReported`. If no existing failure-injection seam fits in a small diff, assert only that the success path stays green and the flag defaults false — do not build new store-mocking machinery.

### 6. Make the Bluetooth Connect row read as clickable (P2-5)
`PopoverController.swift`, `makeBluetoothConnectRow()` (`:2617-2644`):
- Leading glyph: `NSImage(systemSymbolName: "plus.circle", accessibilityDescription: nil)`, `imagePosition = .imageLeading`, `contentTintColor = Tokens.Color.secondaryLabel` (the comment at `:2624-2628` records that `contentTintColor` reliably tints template IMAGES — exactly this case; the title keeps its existing `attributedTitle` treatment).
- Pointer affordance: declare a `private final class PointingHandButton: NSButton` (bottom of `PopoverController.swift`) whose `resetCursorRects()` calls super then `addCursorRect(bounds, cursor: .pointingHand)` — the same idiom as `MainOutRowView.resetCursorRects` (`MainOutRowView.swift:796-799`, read-only reference). Construct the connect button as that class. NOT gold, no pill — the recorded rationale at `:2608-2616` stands; the neutral secondaryLabel tint stays.
- Test: in `PopoverDeviceVisibilityTests` next to the existing `test_fireBluetoothConnectClick()` use at `:253`, assert the mounted connect button carries a non-nil image via a new seam on `PopoverController`: `var test_bluetoothConnectRowHasGlyph: Bool` reading `bluetoothConnectButton?.image != nil`.

### 7. Silence banner: re-theme fix, group role, and a "Try again" action (P1-7 + P1-8 + P2-8)
7a. `SystemAirPlayNoteBannerView.swift`: add `override var wantsUpdateLayer: Bool { true }`. Factor the two color-stamp lines (`:120-121` and the `updateLayer` body `:174-178`) into one private `stampLayerColors()` wrapped in `effectiveAppearance.performAsCurrentDrawingAppearance { … }` (the `ConnectionDiagnosisView.applyBackgroundTint` idiom, `:225-232`), called from init and from `updateLayer()`. Replace `setAccessibilityRole(.staticText)` (`:154`) with `setAccessibilityElement(true)` + `setAccessibilityRole(.group)`; keep the composed `setAccessibilityLabel`.
7b. `SilenceFallbackBannerView.swift`: the same three changes (`wantsUpdateLayer`, `stampLayerColors()` from `:41-42`/`:66-70`, `.group` role at `:57-58`). Additionally give it the note banner's action shape: `typealias Action = SystemAirPlayNoteBannerView.Action`; init grows `action: Action? = nil`; copy the note banner's button construction, width-reserve, trailing-pin layout and target/action dispatch (`SystemAirPlayNoteBannerView.swift:87-104,140-152,161`) — adapted in place, no shared base class. Mirror the note banner's seams: `test_hasActionButton`, `test_tapActionButton` (`:166-170`).
7c. `PopoverPanelViewController.swift`, `setBanner(_:)` (`:1310-1328`): grow the signature to `setBanner(_ text: String?, action: SilenceFallbackBannerView.Action? = nil)` and pass it through. Add seams mirroring the note slot's: `var test_bannerHasActionButton: Bool` and `func test_tapBannerAction()` (keep a weak typed reference to the banner, as `systemAirPlayNoteView` is kept at `:1344`).
7d. `PopoverController.swift`: build the action once as a private computed property — title `"Try again"`, accessibility label `"Try reconnecting to the unreachable speakers"`, handler `{ [weak self] in self?.retryUnreachableMembers() }`. Pass it at BOTH `setBanner` call sites (`:983` and the rebuild tail `:1472`) whenever the banner text is non-nil. Add `private func retryUnreachableMembers()`: for every device in `devicesByID.values` where `groupController?.isMainOutMember(device.id) == true` and `connectionState` is neither `.connected` nor `.connecting`, call `groupController?.requestReconnect(for: device.id)`. This is N deliberate per-device re-kicks through the sanctioned single-device path (`AudioutPopoverUI/AGENTS.md:10` — never a routing re-apply); `MockBackend.retryOutput`'s own guards make extra calls no-ops.
7e. Tests: existing `NoteBannerColorTests` assertions must pass unmodified (the stamp values are identical). Add one test constructing `SilenceFallbackBannerView` with an action and asserting `test_hasActionButton`. In `PopoverControllerTests` (near `localFallbackBannerShowsAndClears`): with a `MockBackend` fleet where a selected member is scripted `.failed`, call `setLocalFallbackActive(true)` on a shown panel, assert `test_bannerHasActionButton`, drive `test_tapBannerAction()`, flush the mock, and assert the failed member left `.failed` (`MockBackend.swift:300-319` flips a still-desired failed device to `.connected`).

### 8. Real empty / searching / permission-denied card states (P1-1)
All in `PopoverController.swift`, using the exact mechanism the Bluetooth Connect row already uses (a subsection whose empty body IS content):
- State model: a private enum `SpeakerSearchState { case searching, noneFound, permissionDenied }` and a private `speakerSearchState() -> SpeakerSearchState?` returning nil whenever the AirPlay OR Cast section of `deviceSections()` has any device; otherwise `.permissionDenied` when `localNetworkDeniedProvider?() == true`; otherwise `.searching` until the grace elapses; then `.noneFound`.
- Host seam: `public var localNetworkDeniedProvider: (() -> Bool)?` — nil/false means "not known denied"; wiring is T7's (Handoff). The variant is dormant until wired; that is intended.
- Grace: private `speakerSearchGraceElapsed = false` and a private one-shot `Timer`, constant `static let speakerSearchGraceSeconds: TimeInterval = 3.0`. `rebuildForOpen()` resets the flag to false and (re)arms the timer only when `speakerSearchState()` is non-nil; `surfaceDidHide()` invalidates it. On fire: set the flag; if `isEffectivelyShown` and the rendered state was `.searching`, run `rebuild()` + `panel.panelContentDidChangeHeight(animated: true)`.
- Rendering: extend `rendersHeader(_:)` (`:1505-1507`) so the AirPlay section ALSO renders its header when `speakerSearchState() != nil` (the "AirPlay Devices" grouping label gives the state line its context — the mirror of the Bluetooth pattern). Extend `addSubsectionRows(_:)` (`:1592-1602`): when the section is AirPlay, empty, and a state is active, `panel.addRow(makeSpeakerSearchStateRow(state))` and return — the exact shape of the BT branch above it. Record the rendered message in a private `renderedSpeakerSearchText: String?` (nil when no state row rendered).
- `makeSpeakerSearchStateRow(_ state:)` builds a wrapper on `PopoverColumnGrid.nameColumnLeading` (copy `makePlaceholderRow`'s constraint shape, `:2583-2602`) with, per state — EXACT copy:
  - `.searching`: label `"Looking for speakers…"` + a trailing quiet indeterminate `NSProgressIndicator` (`style = .spinning`, `controlSize = .small`, start it), spinner omitted entirely when `NSWorkspace.shared.accessibilityDisplayShouldReduceMotion`.
  - `.noneFound`: label `"No AirPlay speakers found on this network."` plus a second smaller hint label under it: `"Make sure your speakers are awake and on the same Wi-Fi network as this Mac."` (hint font `Tokens.Font.captionMedium`). Two labels stacked; the wrapper grows to fit — do NOT hard-pin this variant to `DeviceRowView.rowHeight`.
  - `.permissionDenied`: label `"Audiout doesn't have permission to see devices on this network."` + hint `"Allow Local Network for Audiout in System Settings › Privacy & Security."`.
  - All message labels `Tokens.Font.menuItem` at `Tokens.Color.secondaryLabel` — live state text is never tertiary (`AudioutPopoverUI/AGENTS.md:27`); use the token, not a literal (T9 owns the token's value).
- The Bluetooth Connect row keeps rendering exactly as today — the state line is AirPlay-scoped by copy and by its own subsection header, so the two never contradict (the 2026-08-08 removal at `:1333-1338` was a GENERIC "Looking for devices…" directly over the Connect button; do not remove the BT affordance and do not render a generic line).
- State flips driven by device arrivals/departures already trigger the structural rebuild (the AirPlay header's presence rides `rendersHeader` through `expectedSubsections`); only the timer needs its explicit rebuild above.
- Seams + tests: `public var test_speakerSearchStateText: String?` (returns `renderedSpeakerSearchText`) and `public func test_fireSpeakerSearchGrace()`. New PopoverControllerTests: (1) open with an empty fleet → text is `"Looking for speakers…"` AND the BT connect affordance still renders; (2) `test_fireSpeakerSearchGrace()` → text becomes the noneFound message; (3) an AirPlay device arriving via `update(devices:)` → seam returns nil; (4) `localNetworkDeniedProvider = { true }` + reopen → the permission message; (5) a fleet with an AirPlay device from the start → seam nil and no AirPlay-header-with-no-rows regression (`test_subsectionTitles`).

### 9. Toolbar: separate Quit from Pin, app-quit glyph, named center item (P2-4 + P3-4)
`SurfaceToolbar.swift`:
- `toolbarDefaultItemIdentifiers` (`:199-202`): insert `.space` between `pinItemIdentifier` and `quitItemIdentifier` (least-destructive separation; Quit stays in the toolbar).
- Quit item (`:297-306`): glyph `Self.resolveSymbol("rectangle.portrait.and.arrow.right", fallbacks: ["power"], accessibilityDescription: "Quit")` — an exit-the-app shape, not a speaker-power shape (SF Symbols 3, safe on the macOS 14 floor); `toolTip = "Quit Audiout"`; `label` stays `"Quit"`.
- Centered item (`:283`): `item.label = "Audiout"`.
- `SurfaceToolbarTests.attachingMaterializesTheItemsInOrder` (`:31-43`): update the expected identifier array to include `.space`. Assert the quit item's image is non-nil (seam `test_quitItemHasImage` exists, `:181-183`).

### 10. First-click dead time: 600 ms ceiling + click-cancels-wait (P0-1 — the riskiest edit; smallest possible diff)
Chosen variant (see Verified facts for why "front immediately" is infeasible): keep the deferred-reveal machinery byte-for-byte except:
- `AppSurfaceController.swift:191`: `revealCeiling` 3.0 → **0.6**. Rewrite ONLY its doc comment (`:188-190`) to the new contract: the backstop is now a click-responsiveness bound — warm opens still settle first (quiet window 0.5 s < 0.6 s); a cold open reveals here and the splash covers the content churn while any late frame growth stays bounded and brief.
- `AppSurfaceController.swift:270`: replace `guard !isRevealPending else { return }` with: when `isRevealPending`, call `revealFirstOpen()` and return — a repeat click is the user asking for the surface NOW; `revealFirstOpen()` already fronts at the current measured size, disarms the backstop, and lays the splash (its own `guard isRevealPending` makes the call safe). Update the comment at `:268-269` accordingly.
- Touch NOTHING else on this path: `SurfaceSplashView` timings, hold/ceiling semantics, Reduce Motion and headless branches (`shouldPresent` / `wouldPresent`), `DiscoverySettleTracker`, and the settled-reveal choreography stay exactly as they are — the existing tests at `AppSurfaceControllerTests.swift:925-1048` must pass unmodified.
- New test in `AppSurfaceControllerTests` (inside the existing `asAFirstRealLaunch` harness): `show(anchorRect: nil)` → `test_isRevealPending`; `show(anchorRect: nil)` again → `!test_isRevealPending` and `test_splash != nil` ("a second click cancels the wait and fronts at once").

### 11. Occlusion gating for the pinned-but-covered surface (perf P2-12)
`AppSurfaceController.swift` — the host drives it through the panel's two sanctioned visibility calls (the panel stays host-agnostic; `isEffectivelyShown` remains the one answer, now fed honestly):
- In `init`, register `NotificationCenter.default` observers for `NSWindow.didChangeOcclusionStateNotification` (object: `shell.window`), `NSApplication.didHideNotification`, and `NSApplication.didUnhideNotification`; each handler calls a private `reassessPixelVisibility()`.
- `reassessPixelVisibility()` computes `visible = (shell.window?.occlusionState.contains(.visible) ?? true) && !(NSApp?.isHidden ?? false)` and hands it to a private `applyPixelVisibility(_ visible: Bool)`.
- `applyPixelVisibility` is a LATCH over a new private `surfaceCoveredHidden = false`: guard `isShown`, `selectedScreen == .mixer`, `!isRevealPending`, else return. If `!visible && !surfaceCoveredHidden`: set the latch, `popoverController.surfaceDidHide()`. If `visible && surfaceCoveredHidden`: clear the latch, then the Mixer open ritual the AGENTS mandates for any re-entry (`AudioutPopoverUI/AGENTS.md:29`): `popoverController.rebuildForOpen()`, `popoverController.surfaceDidShow()`, `mixerPanel?.panelContentDidChangeHeight(animated: false)`. (Full `surfaceDidHide` semantics — wizard cancel, transient selection drop — are deliberate: fully covering the surface is the click-away case; a tab switch already does exactly this.)
- Keep the existing lifecycle sites consistent with the latch: `show()` sets `surfaceCoveredHidden = false` on a fresh show; `handleShellClosed()` (`:381-390`) calls `popoverController.surfaceDidHide()` only when `!surfaceCoveredHidden` (then resets the latch); `select(_:)` (`:446-466`) when leaving the Mixer calls `surfaceDidHide()` only when `!surfaceCoveredHidden`, and resets the latch in both directions.
- Test seam: `func test_notePixelVisibility(_ visible: Bool)` → `applyPixelVisibility(visible)` (live occlusion reads never run headless — the notifications don't fire there). New AppSurfaceControllerTests test: show the surface, spy `popoverController.onMeteringActiveChange` transitions and `test_rebuildCount`; `test_notePixelVisibility(false)` → metering false; `test_notePixelVisibility(true)` → metering true and rebuild count increased; a second `test_notePixelVisibility(true)` → no further transition (latch).

## Handoff to other tracks (record in your report; do not do)
- **T3b** (row views): (a) the row-side `isDraggingSlider` clear-on-real-gesture-end fix (`DeviceRowView.swift:1970-1979`, `AppRowView.swift:685-687`); (b) the DeviceRowView half of the blocked-machinery deletion — `.blocked` node style, `isToggleBlocked`, `blockReasonText`, the body-click refusal branch and `test_simulateBlockedBodyClick`, the delegate requirement `deviceRowDidRequestBlockedExplanation` (`DeviceRowView.swift:62`, default at `:3101`) and its two test conformances (`DeviceRowConnectionStateTests.swift:358`, `BTRowsUITests.swift:27`); (c) retiring or repointing the `local-mix-blocked` snapshot scenario (`popover-snapshot/main.swift:876-954`) whose PNGs already render no note — keep its hover fixture.
- **T7** (AppDelegate): wire `popoverController.localNetworkDeniedProvider` to the app's Local Network permission knowledge (`SetupModel.localNetworkStatus`); until then the permission variant simply never renders.

## Out of scope — do not touch
- Everything in the header's DO NOT TOUCH list, plus: `Tokens.swift` contrast repointing (T9), device-row cursor rects/tooltips (P1-2, T3b), `AP1`/`+5` pills (T3b), the diagnosis panel's ✕ (T3b), FoldAnimator/rail/meter perf items (other tracks), `deviceSections()`'s `stateQueue.sync` provider swap (perf P0-1, T1), the note-precedence slot and `statusMasterVolume` regions named in the collision notes.
- No refactors, no shared banner base class, no new abstractions, no error handling for impossible cases, no backwards-compat shims, no comment sweeps, no snapshot regeneration, no AGENTS.md rewrites beyond what a step forces (if a deleted symbol is named in an AGENTS.md, fix that line only — Guard 2 will tell you).
- Do not "improve" the splash timings, the settle tracker, or the one-frame/surplus-shield machinery.
- Do not merge to `main`, do not touch the `main` checkout, no merge without Alec's explicit go-ahead. Push the branch to `origin/claude/fix-popover-entrance` as work progresses.

## Verification

Pre-change baseline (observed 2026-08-27 in this worktree): the five-suite filtered run passed — **262 tests in 5 suites, 52.5 s**.

Run, in order, from the repo root (quoted path), and paste the output tails:
1. `bash scripts/build.sh` → builds clean.
2. `bash scripts/run-tests.sh --filter PopoverControllerTests --filter AppSurfaceControllerTests --filter SurfaceToolbarTests --filter NoteBannerColorTests --filter GroupControllerTests --filter PopoverDeviceVisibilityTests` → all pass; the count should EXCEED the 262 baseline (new tests from Steps 4–11) plus PopoverDeviceVisibilityTests' existing tests.
3. `bash scripts/run-tests.sh` (full suite, once, at the end) → all pass. The full suite can be flaky under machine load (known); one rerun of the failing suite's filter is allowed before reporting a failure as real.
4. Guard 7 requires the staged-diff self-review before commit (`scripts/self-review.sh`) — run it; keep its chatter out of your report.

Done = commands 1–3 ran in your session and passed, with output pasted. Live UI checks (first-click feel, splash-over-growth on a slow network, banner re-theme on a light/dark flip, occluded pinned surface, VoiceOver reaching the banner buttons) are Alec's — list them as OWED in your report; do not claim them.

## Execution plan

- **Track 1 — Steps 1–8, 10, 11 + their tests** (`PopoverController.swift`, `PopoverPanelViewController.swift`, `AppSurfaceController.swift`, both banner views, `GroupController.swift`, forced test/harness files, dev-notes bullet). Model: **opus**. Effort: **high** (dense invariants; the entrance and occlusion edits interact with owner decisions). SERIAL within itself — the steps share `PopoverController.swift`.
- **Track 2 — Step 9 + its test** (`SurfaceToolbar.swift`, `SurfaceToolbarTests.swift` only). Model: **sonnet**. Effort: **low** (mechanical). PARALLEL with Track 1 — file sets fully disjoint.
- The branch has no uncommitted work to depend on; both tracks fork from the branch HEAD. Verification runs once, on the merged result.

## Open decisions (made here; flag to Alec in the report, do not re-litigate)
1. **P0-1 shape:** ceiling 0.6 s + click-cancels-wait, NOT front-immediately (infeasible per the verified `preferredContentSize` behavior). Residual trade: on a cold open the window can grow briefly under the splash while late devices arrive — bounded, and strictly better than 3 s of nothing. If Alec dislikes the growth live, the fallback is raising the ceiling, not new machinery.
2. **Copy:** the hint and permission strings in Step 8 are this order's exact strings; Alec may reword later.
3. **Quit glyph:** `rectangle.portrait.and.arrow.right` (exit shape), `power` as fallback.
4. **"Try again" scope:** Main-Out members not `.connected`/`.connecting`, each through `requestReconnect` (per-device, sanctioned path).
5. **Occlusion = click-away semantics:** full `surfaceDidHide` teardown (wizard cancel included) on a fully-covered surface, matching the existing tab-switch behavior.
6. **Permission variant stays dormant** until T7 wires the provider.

## Executor rules (copy verbatim into the handoff prompt)
> - Follow the steps in order. Do not add, merge, reorder, or skip steps.
> - If reality contradicts a Verified fact or a step is impossible as written, STOP and report the discrepancy. Do not improvise a workaround.
> - Before reporting progress, audit each claim against a tool result from this session. Only report work you can point to evidence for. If tests fail, say so with the output.
> - "Done" means the Verification commands were run in this session and passed. Paste their output.
> - Touch nothing in the Out-of-scope list.
> - Deliver what was asked, at the scope intended. If the spec seems mistaken or a better approach exists, say so in a sentence and continue as specified rather than quietly narrowing, widening, or transforming it.

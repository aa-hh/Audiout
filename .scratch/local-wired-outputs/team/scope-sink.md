## Answers

**Q1 — `applyWiredSnapshots` context (03 cross-area Q1).** Confirmed. `/Users/alechenderson/Projects/AirPlay Controller/.claude/worktrees/cpi-consumption-analysis-2e6406/AudioutCore/Sources/AudioutCore/NativeBackend+Wired.swift:3` is `extension NativeBackend`, `:14` is `func applyWiredSnapshots(_ snapshots: [WiredOutputSnapshot]) {   // on stateQueue`, and the only caller dispatches it on that queue: `NativeBackend.swift:2081` `self?.stateQueue.async { self?.applyWiredSnapshots(snapshots) }`. All three members are internal stored vars on the same class: `expectedSelected` `NativeBackend.swift:876`, `btSelectedUIDs` `:622`, `btPerAppClaimedUIDs` `:634`. The file as built removes the row on unplug (`:33-40`: `isAvailable = false`, `commitKnownDevice`, `known[id] = nil`, `order.removeAll`, `emit(.deviceRemoved)`), and its update branch (`:16-19`) writes only `name` and `wiredTransport`, never `isAvailable = true`.

**Q2 — backend half of the new keep-while-used rule.**

What the backend can read for "used", all on `stateQueue`, no injected closure needed:
- Selected: `expectedSelected.contains(id)` (`NativeBackend.swift:876`; written kind-blind at `NativeBackend+Tone.swift:333`).
- Routed by an app (a `.device` route, or a member of an app-routed `.group`): `routesTargetDeviceLocked(id)` (`NativeBackend+PerAppRouting.swift:71-78`), which reads `lastRoutes` (`NativeBackend.swift:1384`), the raw intent table. Do not use `btPerAppClaimedUIDs` for this: it is post-eligibility, and a route to an unavailable row is already demoted out of it.
- Member of any saved group: `lastGroupTargets.values.contains { $0.memberVolumes[id] != nil }` (`NativeBackend.swift:1386-1391`, "The saved groups' resolved per-app-route memberships"). It is pushed from `AppDelegate.swift:2488-2493` as `resolveGroupTargets(groupController.groups, devices:)`, and `AppRoutingController.swift:246-253` emits one entry per saved group filtered only by `canBePerAppRouteTarget()` (which admits `.wired`, `AppRouteTargetEligibility.swift:28-32`), not by availability. So NativeBackend never sees `GroupStore` directly (grep over `NativeBackend*.swift` hits comments only), but it already holds the membership it needs.

Rule: `wiredRowIsUsedLocked(id) = expectedSelected.contains(id) || routesTargetDeviceLocked(id) || lastGroupTargets.values.contains { $0.memberVolumes[id] != nil }`.

Re-evaluation sites (a kept greyed row is pruned when it stops being used): one helper `pruneUnusedWiredLocked()` on `stateQueue` that, for every `id in order where known[id]?.kind == .wired && known[id]?.isAvailable == false && !wiredRowIsUsedLocked(id)`, does `known[id] = nil`, `order.removeAll { $0 == id }`, `emit(.deviceRemoved(id: id))`. Call it from (1) the end of `applyWiredSnapshots`, (2) `setOutputSet` right after `self.expectedSelected = ids` (`NativeBackend+Tone.swift:333`), (3) `updateAppRoutes` right after `self.lastGroupTargets = groupTargets` (`NativeBackend+PerAppRouting.swift:254`). Site 3 covers both "route cleared" and "group deleted/edited" because a group edit re-pushes through `pushAppRoutesToBackend()` (`AppDelegate.swift:2465`, `:2488`).

Sink set: an unavailable wired UID drops from the sink set by construction. `applyBTSinkTransition` re-resolves every UID at each apply (`NativeBackend+Bluetooth.swift:459-462`: `btDeviceIDForUID?(uid) ?? aggregateControl.resolveDeviceID(forUID: uid)`, nil → no spec) and `BTSyncedSink.setDevices` stops every sink whose UID is absent (`BTSyncedSink.swift:1435-1438`). The trigger is the `reapplyBTSinkLocked()` call specified in the corrected 03 step 7 below.

One gap the app layer must close (not backend code, flagged to scope-routing): `AppDelegate.swift:2753` only re-pushes group memberships for a new device `if isNew, hasGroupRoute`, so a wired row plugged in after launch is absent from `lastGroupTargets` until some route or group edit, and its unplug would read "not in a group". Drop `hasGroupRoute` from that guard.

**Q3 — 03's "Interface for other tickets" claims.** All hold; sent to scope-ui.
- Trim/reset: `setBTSyncTrim` `NativeBackend+Bluetooth.swift:1285-1311` writes `btTrimsByUID[id]` and pushes `setTrimMs(_, forDeviceUID: id)`; `btSyncTrim` `:1313`, `btHasSyncTrim` `:1317`, `resetBTAlignment` `:1321-1342`, `btUsableTrimRangeMs` `:2450-2453`. No kind guard on any.
- Latency/wizard: `btMeasuredLatencyMs` `:2464`, `btWizardLatencyRangeMs` `:2468`, `setBTWizardLatencyPreview` `:2501-2530` (`btSink?.setOffsetMs(value, forDeviceUID: id)`), `endBTWizardLatencyPreview` `:2532-2560`, `setBTWizardTrimPreview` `:1348`, `endBTWizardTrimPreview` `:1355`, `setBTWizardTickActive` `:1366-1410`, `stageBTMicProbe` `:1447-1450`. No kind guard on any.
- Tick timbre: `AlignmentTickInjector.swift:635-637` binds `brightTick` to the "bluetooth" variant; `NativeCaptureCoordinator.swift:1590` passes that variant as `btPCM`; `:1987-2004` writes `btPCM ?? pcm` into `snapshot.btSink`. Wired sinks live inside that manager after 03, so they hear the bright click. 04's `targetIsBluetooth: isBluetooth || isWired` is right.

**Q4 — 05's Q1/Q2.** Sent to scope-routing. 03 reuses the manager (option a). The three widenings are sufficient: `NativeBackend+PerAppRouting.swift:30` (`outputIDs[id] != nil || device.isBluetooth`), `:1747` (`.filter { self.known[$0]?.isBluetooth == true }` → `perAppBTUIDs`), `:1781` (`set.deviceIDs.filter { known[$0]?.isBluetooth == true }`). Downstream is UID-string only: `btPerAppClaimedUIDs` `NativeBackend.swift:634`, `btArmingLocked()` `NativeBackend+Bluetooth.swift:219-222`, `btSink?.setPerAppClaimedUIDs` `NativeBackend+PerAppRouting.swift:1763-1766`, `btPerAppFeeds` `:1789`. Observer: `AudioutCore/Tests/AudioutCoreTests/NativeBackendBTSelectionTests.swift` `SpyBTSink.setPerAppClaimedUIDs` records into `perAppClaimedUIDCalls` (`:198-199`, `:238-239`), asserted at `:1254-1312`. 03 step 1 copies that spy into `NativeBackendWiredSinkTests.swift`.

## Corrections to the draft

**Ticket 02 amendment (replaces `NativeBackend+Wired.swift:31-40` and fixes `:16-19`).** Corrected step text:
> In `applyWiredSnapshots`, update branch (`:16-19`): also set `device.isAvailable = true`, still committing through `commitKnownDevice` (`NativeBackend.swift:4877-4882`), which samples `isRouteTargetEligibleLocked` before/after and calls `rerunAppRoutesIfTargeted`; that is what restores a demoted per-app route on replug and demotes it on unplug (the Bluetooth path at `NativeBackend+Bluetooth.swift:734`). If the row went unavailable→available and `expectedSelected.contains(id)`, call `beginBTConnectingLocked(id)`. Removal loop: for each `id in order where known[id]?.kind == .wired && !present.contains($0)`, guard `device.isAvailable` (skip an already-greyed row), set `isAvailable = false`, set `connectionState = .off` unless it is `.failed` (mirror `NativeBackend+Tone.swift:524-528`), `commitKnownDevice(id, device)`. Do NOT remove or emit `deviceRemoved` here. Then call `pruneUnusedWiredLocked()` (defined in Q2), which removes only rows that are unavailable and not used. Add `pruneUnusedWiredLocked()` calls after `NativeBackend+Tone.swift:333` and `NativeBackend+PerAppRouting.swift:254`. Update the doc comment at `:9-13` (it currently says rows leave on unplug because a kept row would be a ghost).
> `AppDelegate.swift:2753`: `if isNew { pushAppRoutesToBackend() }` (drop `hasGroupRoute`), so a wired row that arrives after launch enters `lastGroupTargets`.
> Tests in `NativeBackendWiredDevicesTests.swift`: keep `unpluggedOutputLeavesTheList` (`:175-208`) for an unused row. Add `usedUnpluggedRowStaysGreyedUntilReleased` — defect: an unplugged selected headphone row vanishing and taking its selection with it. `setOutputSet([jack.id])`, fire `[dac]`, wait for `jack` with `isAvailable == false` and `connectionState == .off`, expect no `.deviceRemoved(jack)`; `setOutputSet([])`, wait for `.deviceRemoved(jack)`. Add `replugRestoresAvailability` — fire `[dac, jack]` again while selected, expect `isAvailable == true`.

**03 step 7** (wrong: assumes add/remove). Corrected: in the unplug loop, set `desiredAvailabilityMoved = true` when `expectedSelected.contains(id) || btPerAppClaimedUIDs.contains(id)`; in the update branch, the same when the row went unavailable→available. After both loops, `if desiredAvailabilityMoved { reconcileSilenceWatchdog(); reapplyBTSinkLocked() }`, mirroring `NativeBackend+Bluetooth.swift:733, 747-750`. Then `pruneUnusedWiredLocked()`.

**03 step 3, three additions in `NativeBackend+Bluetooth.swift` (nobody owned them; it is 03's file).**
- `:1399` arm gate: `Set(self.btSelectedUIDs.filter { self.known[$0]?.isAvailable == true })`. Otherwise a selected-but-unplugged wired row (now the ordinary state) sits in `btSelectedUIDs`, never renders, and every wizard run plays bed for the full `wizardArmCeilingSeconds` (`NativeBackend.swift:668`).
- `:1539` `referenceIsBluetooth`: widen to `isBluetooth == true || isWired == true`. A wired reference is inside the manager and needs the sweep-free feed (`:1551-1552` `referenceOnEngine: !referenceIsBluetooth`, `downWindowUID`). Behaviour already comes out staggered via `otherBTAudible`; this is the honest condition.
- `:1067-1070` `DriftCorrectionApplier`'s `isBluetooth` closure: widen to `isBluetooth == true || isWired == true`. Once step 4 puts wired ids in `btSelectedUIDs`, `refreshDriftTrackingLocked` (`:1176-1178`) builds a `.bluetooth` baseline for any selected uid with a measured latency (`btDriftBaselines` `:1130-1137`), so a wizard-measured wired row is sampled, then every correction is refused at `DriftCorrectionApplier.swift:176` and logged per window. Widening is right: the wired sink is the same delay line and `writeBTDriftLatency` is uid-keyed. This overrides 03's "wired rows are excluded from drift anchors" only for being a drift *target*; the anchor exclusion at `:1190` stays.

**03 step 1** `unpluggedWiredRowLeavesTheSinkSet`: still valid as written (the retained selected row's UID no longer resolves, so `deviceSets.last == []`), but the ticket 02 harness now emits no `deviceRemoved` for it; do not wait on one.

**03 "Interface" line "Ticket 04 … the drift applier's `isBluetooth` closure … decide"**: delete; it is 03's, above.

## Sent to teammates

- scope-ui: Q1/Q2/Q3 answers with lines; the deselect-on-loss edge widened to wired would defeat "stays greyed while selected" (backend prunes the row once deselected unless routed/grouped); route reset at `PopoverController.swift:944-961` never fires for wired.
- scope-routing: 05 Q1/Q2 answers, spy location; then a retraction of my proposed `setSavedGroupMemberIDs` after verifying `lastGroupTargets` covers every saved group, plus the `AppDelegate.swift:2753` `hasGroupRoute` fix.

## Still open

- Owner/scoper call: 04's deselect-on-loss edge must stay Bluetooth-only for "selected" to ever hold a greyed wired row; scope-ui, scope-routing and I all recommend that. If the scoper keeps the edge for wired, the retention rule collapses to routed-or-group-member.
- Wired hardware volume: a kept-unavailable wired row has no `AudioObjectID`; volume/mute stay software-gain only (03 decision), nothing to check.
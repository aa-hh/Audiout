# Phase 2 — device detail + icons
- T2: added DeviceIconStore.swift (versioned JSON, [deviceID:symbolName], mirrors ExcludedAppsStore); swift build green
- T1: added optional Group.iconSymbolName + defaultIconSymbolName to GroupStore.swift (back-compat, no schema bump); build/tests green
- T3: added DeviceIcon.swift (DeviceIcon namespace + DeviceIconController) in AudioutedSharedUI; swift build green
- T6: SidebarViewController now resolves device icons via deviceIconController?.symbolName(for:) and group icons via DeviceIcon.resolve(group.iconSymbolName, default: Group.defaultIconSymbolName); build green, window-harness 40/40 pass.
- T4: IconPickerViewController.swift created (curated NSGridView grid + live-validated search/preview + Use default icon; swift build green)
- T5: DeviceRowView.apply gained optional iconSymbolName param (resolves via DeviceIcon.resolve, default nil = unchanged behavior); build/443 tests/73 popover-harness checks all green, zero caller changes.
- T15: added DeviceIconStoreTests, GroupIconPersistenceTests, DeviceIconResolverTests (21 new tests); swift test green, 464 total, 0 failures
- T7: MembershipRowView.swift gained optional iconSymbolName param (resolves via DeviceIcon.resolve, default nil = unchanged behavior); swift build green, swift test green (464 total, 0 failures)
- T8: add deviceIconController var to MixerViewController, wire iconSymbolName to row.apply()
- T-POPGROUP: GroupRowView now shows the group's resolved icon (DeviceIcon.resolve, secondaryLabelColor tint) between chevron and name; build+popover-harness green (73/73).
- T9: PopoverController now injects deviceIconController and passes iconSymbolName into both applySelectionState row.apply call sites, chaining any existing onChange to refreshDeviceRows(); build/test green, popover-harness 73/73.
- T10: added DeviceDetailViewController.swift (read-only device detail pane: large icon + Contacts-style hover-scrim icon picker, metadata form, group-membership list); swift build green.
- T13: GroupCreationSheetController gets icon well (bordered square -> anchored IconPickerViewController, default Group.defaultIconSymbolName) threaded into createGroup(iconSymbolName:); GroupController.createGroup adds iconSymbolName param (dedup path preserves existing group's icon); MembershipRowView rows in sheet resolve via optional injected DeviceIconController; test_iconWellSymbolName/test_pickIcon added; swift build/test/window-harness all green.
- T12: added icon well (bordered NSButton) beside Name field in GroupEditorViewController, presenting IconPickerViewController as anchored popover, instant-persist via saveGroup, wired DeviceIconController into MembershipRowView icons; swift build/test green (464 tests, pre-existing flaky NativeBackendTests.testMuteStashAndRestore passes in isolation), window-harness exits 0 (40/40 checks).
- T11: MixerWindowController integrates DeviceIconController (injected into sidebar/mixer/editor/sheet/detail, onChange chained to refreshAll) and DeviceDetailViewController (.device selection shows detail pane via showDetail, refreshAll re-renders/falls back to mixer); test_detail + test_isShowingDetail added; build+464 tests+40 harness checks green
- T14: shared DeviceIconController (loadPersisted:true) constructed once in AppDelegate and injected into both PopoverController and MixerWindowController; swift build green
- T19: updated AGENTS.md docs in AudioutedWindowUI/AudioutedCore/AudioutedSharedUI for DeviceDetailViewController, IconPickerViewController, DeviceIcon/DeviceIconController/DeviceIconStore, and Group.iconSymbolName; all backticked symbols verified via git grep --untracked (files are new/untracked in this worktree).
- T18: added window-harness section [9] (select device -> detail pane, membership text, activeGroupID untouched, deselect->mixer) and window-snapshot state 4-device-detail (icon override + hover scrim forced visible, light+dark); swift build green, harness exits 0 (49/49 checks), snapshot writes 8 PNGs.
- T17: Added PopoverIconTests.swift (8 tests) covering DeviceIconController override rendering on popover device rows, no-controller/no-override fallback to kind default, GroupRowView iconSymbolName resolution (set/unset/stale), and onChange-driven refresh; swift test green (472/0), popover-harness exits 0 (73/73).
- T16: added DeviceDetailViewTests.swift (23 tests) + IconPickerTests.swift (15 tests), extended MixerWindowControllerTests.swift with 10 new cases (device-select→detail wiring, detail refresh/fallback, editor/create-sheet/dedup icon persistence, DeviceIconController override reaching a mixer DeviceRowView); swift test green (520 tests, 0 failures).
- T20-VERIFY: full gate PASS — build clean, swift test 520/520 (2 skipped), window-harness 49/49, popover-harness 73/73, window-snapshot produced all 8 PNGs; visual review of light/dark states 1-4 found no regressions

# Playback meters — real per-app/per-device/main-out sources
- T2 done: Added meterWidth, meterToLeading, firstElementLeading(indented:) to PopoverColumnGrid - verify pass
- T4b done: Reserved-empty meter column in AppRowView, shifted leading icon anchor rightward using PopoverColumnGrid.firstElementLeading(indented:false) - verify pass
- T1 done: Added LevelMeterView (layer-backed VU bar, CVDisplayLink ballistics, self-stopping at rest, Reduce Motion snap, test_setDisplayedLevel hook) to AirPlayControllerSharedUI - verify pass
- T4a done: MainOutRowView gets leading meter column (icon repointed via PopoverColumnGrid.firstElementLeading), live LevelMeterView mounted + public setLevel(_:) forwarding - verify pass
- T3 done: DeviceRowView gets showsMeter param (default false), leading LevelMeterView mounted only when true with icon repointed off meter.trailingAnchor, public setLevel(_:) forwarding, apply() resets meter when row isn't a playing selected+unmuted output, test_meterLevel() hook - verify pass
- T5 done: PopoverController.updateLevel(_:for:)/test_pushLevel(_:for:) dispatch to device row + Main Out master (when id is currently selected); popoverDidClose resets every device row meter and the Main Out meter via new DeviceRowView.resetLevel()/MainOutRowView.resetLevel() - verify pass
- T6 done: AppDelegate .level case now forwards level to UI via popoverController.updateLevel(rms, for: id) while keeping AIRPLAY_DEBUG_LEVELS throttled log - verify pass
- T7 done: Added meter-ON popover-snapshot variant (popover-meters-{light,dark}.png) pushing fixed RMS via test_pushLevel + settling LevelMeterView.test_setDisplayedLevel synchronously - verify pass
- T8 done: Added LevelMeterView ballistics unit tests, DeviceRowView setLevel/test_meterLevel + apply-resets-meter tests, PopoverController test_pushLevel/popoverDidClose meter tests - verify partial (2 PopoverController tests fail: makeDeviceRow never passes showsMeter:true so popover device rows never actually get a live meter; flagged as separate task, not fixed here since T8 is test-only)
- T-GATE done: Added MeteringControlling optional capability protocol (mirrors LatencyConfigurable); NativeCaptureCoordinator gates RMS computation+onLevel on setMeteringActive (default inactive); MockBackend starts/stops its levelTimer on the same flag; PopoverController.onMeteringActiveChange fires true/false on popoverDidShow/popoverDidClose, wired in AppDelegate to (backend as? MeteringControlling)?.setMeteringActive; NativeBackend.stop() and MockBackend.stop() leave metering inactive - verify pass
- T9 done: documented LevelMeterView/meter column/PopoverColumnGrid in SharedUI+PopoverUI AGENTS.md, marked PLAN-PHASE-1 Q8 re-enabled, playback-meter-research.md Phase B obsolete, p2b-multistream-brief future-seam note - verify pass
- T-FIX done: PopoverController.makeDeviceRow now passes showsMeter:true so popover device rows actually mount the meter (gap between T3/T5); fixes the 2 failing PopoverController meter tests - verify pass

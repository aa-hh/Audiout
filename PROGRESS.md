- [T9] Removed AP1 'coming soon' explanation popover (property, presenter, test hook, delegate impl) from PopoverController.swift — verified no dangling references to deleted symbols remain in the file (grep clean); DeviceRowView still declares the delegate requirement w/ default no-op impl (T8's scope).
- [T2] Refreshed stale airplay.c line-anchor comments in NativeDiscovery.swift classify()/parseFeatures() (4139-4147 gate, features_map ~412/438, features_parse ~3959); logic untouched — build currently broken by unrelated concurrent edits to DeviceRowView.swift/PopoverController.swift in this shared worktree, so classification tests could not run locally; verify deferred to barrier.
- [T8] Made AP1 device rows fully live in DeviceRowView.swift (removed isUnsupported gate, checkbox/slider/mute/alpha/accessibility all treat AP1 as normal supported; deleted unsupportedExplanation, the deviceRowDidRequestUnsupportedExplanation delegate requirement + default no-op, test_isUnsupported, test_simulateClick, and the unsupported-click mouseDown override) — verified by read-back grep for isUnsupported/unsupportedExplanation/deviceRowDidRequestUnsupportedExplanation/test_simulateClick in this file (none remain); found ONE dangling caller left over in PopoverController.swift:1287 (`test_clickDeviceRow` calls the now-deleted `test_simulateClick()`) that T9 must remove.
- [T1] Cloned OwnTone 29.2 into dev/owntone-src (git-ignored) and wrote AirPlayEngine/docs/raop-seam-brief.md (full raop.c external-symbol classification, RSA-AES handshake, inline uncompressed-ALAC encoder, NTP timing/RTP model, two-backend outputs dispatch + callback-count contract, output_raop shape) — verified against a full read of raop.c and the existing shim surface (grep-confirmed safe_atoi32/b64_encode/L_RAOP are the only genuinely-new shim symbols; every heavy dep already vendored/linked).
- [T3] Vendored OwnTone 29.2 raop.c into AirPlayEngine/Sources/CAirPlayEngine/sender/ (byte-identical but one marked, ledgered ffmpeg-shed guard in master_session_make — VENDORED-DIFFS Entry 3); includes resolve to existing shims via header-search paths, metadata/db/artwork hit no-op shims (cut as airplay.c did); added shims/misc safe_atoi32 + self-contained RFC-4648 b64_encode (RSA-AES SDP handshake) and shims/conffile uncompressed_alac=true (inline uncompressed-ALAC, no ffmpeg on the RAOP path); exposed extern output_raop via engine_bridge.h; two-backend outputs dispatch + engine_bridge dual-callback wiring deferred (brief §6). — `swift build` clean with raop.c linked; `swift test` 126/126 (was 119; +7 RaopBackendTests covering output_raop shape, raop_volume_to_pct parse, b64_encode, safe_atoi32); all pre-existing AP2 tests green.
- [T4] Added two-backend per-device dispatch to shims/outputs.{c,h}: backend_for(device->type) selects &output_airplay (AP2) or &output_raop (AP1); new outputs_device_start/stop/flush/probe/volume_set/authorize/cb_set/free_extra wrappers forward and preserve return value N (unhandled type logs + returns -1/no-op); outputs_write broadcasts to BOTH backends (each self-filters by quality/stream_id, §6.3); outputs_device_free's inline free_extra now dispatches by type instead of hardcoding output_airplay; added outputs_backend_definition_for test seam. — `swift build` clean; `swift test` 128/128 (was 126; +2 RaopBackendTests asserting AP1->output_raop and AP2->output_airplay routing by pointer identity); callback accounting unchanged/backend-agnostic (wrappers only pass N through), AP2 path byte-for-byte equivalent (type AIRPLAY still resolves to output_airplay).
- [T5] Added the RAOP discovery-feed symbol pair (airplayengine_raop_discovery_ready/airplayengine_feed_raop_device) to engine_bridge.{h,c}, mirroring airplayengine_discovery_ready/airplayengine_feed_device exactly but targeting raop_device_cb; updated shims/mdns.c to key mdns_browse's capture on `type` so airplay_init's and raop_init's callbacks (previously one shared variable) coexist instead of the later init clobbering the earlier one; Package.swift needed no changes (raop.c already compiles via the existing `sources: ["sender", ...]` directory listing, and gcrypt/ffmpeg linker flags are unchanged/reused, no ffmpeg added). — `swift build` and `swift build --build-tests` clean; `swift test` 128/128 (unchanged count — no new test file added this task, existing RaopBackendTests/AirPlayEngineTests all still pass, confirming the new symbols link and nothing regressed).
- [T6] Added DeviceDescriptor.ServiceKind (.raop/.airplay, defaulting to .airplay) to AirPlayTypes.swift and routed feedDescriptor() in AirPlayEngine.swift to airplayengine_feed_raop_device (kind: .raop) vs the existing airplayengine_feed_device (kind: .airplay), on the engine thread via the same t.run marshaling; addOutput/write(pcm:)/setVolume untouched (shared registry still fans out by device->type). — `swift build` clean; `swift test` 130/130 (was 128; +2 tests: default-kind-is-airplay, and an AP1 descriptor parses its deviceid + feeds the RAOP path without crashing — real output_raop registry insertion still requires the gated live run since no raop_init has captured raop_device_cb in the headless suite).
- [T7] Un-gated AirPlay-1 in NativeBackend.swift so AP1 receivers drive through the SAME shared engine as AP2 (no separate path): mapDiscovered/merge now surface AP1 available; discovery feeds every reachable receiver (renamed feedEngineIfAP2→feedEngineIfAvailable, guarding on isAvailable) and addOutput-s AP1 on select; the select guard, capture gate, and disappear-teardown key on !isLocalDevice instead of supportsAirPlay2; deleted the vestigial AirPlay1Sending seam. supportsAirPlay2 STAYS false for AP1 (means 'no perfect sync') — it just stops being an unsupported gate; sticky-AP2 and per-app stream_id paths untouched. — swift build clean; rewrote the 3 AP1-old-contract tests + added testAirPlay1ConvergesAndDrives/testCaptureStartsOnAP1Selection/testAvailableUpdateKeepsEngineSession; full suite 763 tests, 2 skipped, 0 failures (sticky-AP2 tests unchanged-green).
- [T10] Added DeviceRowAirPlay1LiveTests.swift asserting an AP1 row renders full-alpha/enabled with drivable checkbox/slider/mute and a normal (non-"unsupported") accessibility label; updated MockBackend.demoFleetWithAP1Only doc comment and the stale isUnsupported doc comment in NativeBackendTests.swift to reflect the live AP1 behavior (T7 already covered AP1 add/converge/volume/mute/capture). — swift test 767/767 (2 skipped), 0 failures; sticky-AP2 + classification tests unchanged-green.
- [T12] Updated docs to match the landed AP1/raop.c work: VENDORED-DIFFS.md Entry 3's "NOT done in T3" note now credits T4 (two-backend outputs.c dispatch)/T5 (engine_bridge dual-callback capture)/T6 (DeviceDescriptor.ServiceKind routing)/T7 (NativeBackend driving AP1 live); seam-map.md gained a RAOP addendum superseding its Appendix A "CUT raop.c" verdict, pointing to raop-seam-brief.md + VENDORED-DIFFS Entry 3; AirPlayEngine/AGENTS.md's stale "Not fully wired into the app" bullet replaced with a two-backend-dispatch rule + a Map row for raop-seam-brief.md; first-light-report.md's "What's next" AP1 framing fixed (D6 gate retired, not deferred) and a new "AP1 (RAOP) first light — pending" stub section added for the user's still-owed by-ear test. license-inventory.md's raop.c GPL row was already current (T3); root/AudiouterCore AGENTS.md have no stale AP1 framing (checked, no edit needed). — verified every backticked symbol added (output_raop, output_airplay, backend_for, outputs_write, DeviceDescriptor/ServiceKind, airplayengine_feed_raop_device, airplayengine_raop_discovery_ready, NativeBackend, supportsAirPlay2) resolves via `git grep` against tracked *.swift/*.c/*.h.
- [RAOP-name-fix] Fixed the RAOP engine-feed contract so real AP1 hardware can register: the `DeviceDescriptor` is engine-facing (`raop_device_cb` derives the device id from the instance NAME via `safe_hextou64`+`strchr(name,'@')`, reading no `deviceid` TXT), so it must carry the raw `"<12-hex>@<display>"` name — but `NativeDiscovery.buildDevice` was stripping that prefix (a leftover from the pre-T7 assumption that raop-only descriptors are never fed to the engine, which T7's AP1 un-gating invalidated). Changed: (1) `NativeDiscovery.swift` buildDevice — removed the `built.name = strippedRaopDisplayName(built.name)` mutation and rewrote the justifying comment (descriptor now keeps the raw name; sticky-AP2-offline branch reuses its cached AP2 descriptor untouched); (2) `NativeBackend.swift` mapDiscovered (~:2579) — user-visible `Device.name` now = `NativeDiscovery.strippedRaopDisplayName(descriptor.name)` (no-op for AP2/local), moving the display strip downstream; (3) `NativeBackend.swift` kind(for:) (~:2634) — same strip on the name feeding the Kind substring heuristics so hex digits can't pollute them. `strippedRaopDisplayName` is already module-internal static, callable from NativeBackend as-is. Tests: reconciled `testRaopNameDerivedIDSurfacesAsAP1Only`, `testRaopOnlyDisplayNameStripsMACPrefix`, `testDualAdvertisedKeepsAirplayNameOverRaopPrefixedName` to the new contract (descriptor.name RAW, display via strippedRaopDisplayName); ADDED regression guard `testRaopOnlyDescriptorKeepsRawHexPrefixForEngineIDParse` asserting a `_raop._tcp`-only descriptor.name still has the 12-hex `@` prefix AND that its parsed hex == the derived OutputID (the sync invariant). — `swift build` clean; `swift test --filter 'NativeDiscoveryTests|NativeBackendTests'` 133/133 0 failures incl. every sticky-AP2 test (testAP2GoingOfflineStaysAP2Unavailable, testAP2OfflineThenBack*, testAP2LosingAirplayAdvertGoesOfflineNotAP1, *DedupesToOneAP2) unchanged-green; AirPlayEngine `RaopBackendTests` 9/9. NOTE (adjacent, NOT in this fix's scope): the live `NativeDiscovery.descriptor(from:)` never sets `kind: .raop` (defaults `.airplay`) and `updateDiscovery` requires `descriptor.parsedID` which reads only the `deviceid` TXT (nil for name-derived shairport-sync gear) — so the live AP1 feed still needs kind=.raop wiring + a name-derived parsedID fallback before real name-only RAOP hardware fully connects; engine-probe (T11-redo) sidesteps both by hand-building the descriptor.
- [T11 redo] Added a `--raop` per-device flag to engine-probe (ProbeArgParsing.swift + main.swift), gated by the same `--i-have-a-receiver-and-owntone-is-stopped` flag: builds a genuinely `.raop`-kind DeviceDescriptor (no features/model TXT — real `_raop._tcp` TXT has neither; `name` encoded as `<hex-id-no-colons>@<device-name>` matching real Bonjour RAOP instance naming, since raop_device_cb parses the id out of `name` itself via safe_hextou64, not a TXT key — the included `deviceid` TXT is Swift-side-only bookkeeping for DeviceDescriptor.parsedID/OutputID and must stay in sync with the name's hex prefix) then feeds/addOutput/setVolume/writes/stops through the same flow as AP2; prior attempt's "architecturally blocked" verdict was false (stale docs) — .raop routing, output_raop wiring, and DeviceDescriptor.kind all verified present in source before editing. — `swift build` clean; `swift test` 132/132 (was 130; +2 new EngineProbeParsingTests for --raop per-device/default grouping); CLI invocation for the user: `engine-probe --raop --address <ip> --port 5000 --device-id <AA:BB:..> --pcm <file.raw> --i-have-a-receiver-and-owntone-is-stopped` (classic RAOP receivers usually listen on port 5000, not AP2's 7000); verified headless dry-run (no gate flag) prints the plan tagged "RAOP/AirPlay-1" and exits 0 without opening a socket, incl. a mixed AP2+RAOP multi-device plan; the actual RTSP handshake against real hardware remains the user's by-ear gated test, per the probe's purpose.
- [RAOP-live-feed-fix] Closed the two confirmed live-path gaps that blocked real classic-RAOP (name-only) hardware from ever registering, in ONE funnel — `NativeDiscovery.descriptor(from:)` (the sole `DeviceDescriptor` construction in AudiouterCore Sources): (Gap A) set `kind: service.serviceType == .raop ? .raop : .airplay` so `feedDescriptor` routes a `_raop._tcp` device to `airplayengine_feed_raop_device`/`output_raop` instead of the AP2 gate (it defaulted `.airplay`); (Gap B) for a `.raop` service with NO `deviceid`/`DeviceID` TXT, inject `txtRecord["deviceid"] = deriveIDFromInstanceName(service.name)?.id` (canonical uppercase colon-hex) so `updateDiscovery`'s `guard descriptor.parsedID` no longer throws `invalidDescriptor` — real shairport-sync/AirPort-Express gear carries no `deviceid` TXT. Verified three-way sync in a code comment: Swift `OutputID`, `parsedID` (colon-stripped), and C-side `safe_hextou64("6B2E52B73717")` all equal `0x6B2E52B73717`; injection is skipped for AP2 (real deviceid) and never overrides an existing `deviceid`. Traced the rest of the live AP1 path (T7's work) — NO further gaps: `feedEngineIfAvailable` feeds `discovered.descriptor` verbatim; `convergeDevice`/`descriptorToFeed`/`lastDescriptors[id]` all carry the same descriptor; `setOutputSet`'s converge guard, `setVolume`/`setMuted`, and the capture gate key on `!isLocalDevice`/`outputIDs[id]`, never `supportsAirPlay2`; `mapDiscovered` surfaces AP1 available + `supportsAirPlay2=false` and strips the display name downstream. Sticky-AP2 preserved: `buildDevice` prefers the `_airplay._tcp` source and the offline branch reuses the cached AP2 descriptor, so the injection only fires for a raop-only source. Files changed: `Sources/AudiouterCore/NativeDiscovery.swift` (the fix + rationale comment); `Tests/AudiouterCoreTests/NativeBackendTests.swift` (SpyEngine now captures full fed descriptors via `fedDescriptors`/`fedDescriptorList`; new `ScriptedBrowser`+`DiscoveredBox` doubles; new end-to-end test). Integration test: `testClassicRaopDiscoversAndFeedsEngineAsRaopEndToEnd` — drives a realistic classic-RAOP `ResolvedService` (name `"6B2E52B73717@Kitchen Speaker"`, `_raop._tcp`, empty TXT) through the ACTUAL NativeDiscovery→descriptor production and on through NativeBackend converge→engine feed, asserting `descriptor.kind == .raop`, raw `"<12hex>@…"` name preserved, `parsedID != nil` == name-derived `OutputID` (0x6B2E52B73717), injected `deviceid == "6B:2E:52:B7:37:17"`, AP1-only + available, stripped display name "Kitchen Speaker", and that the ENGINE-received descriptor still has `kind == .raop` (routes output_raop) and is addOutput-ed. Verify: `swift build` clean; `swift test --filter 'NativeDiscoveryTests|NativeBackendTests'` 134/134 0 failures (incl. all sticky-AP2 tests + the new integration test); AirPlayEngine `swift test` 132/132 0 failures (incl. 10 RaopBackendTests, no AP2 regression).
- [T2-diag-logging] Added TEMPORARY diagnostic logging to `NativeDiscovery.handleResolve()` to track device-discovery duplicate-row bug: every resolved `_raop._tcp` or `_airplay._tcp` service now logs `[Audiouter] NativeDiscovery.DIAG: resolved name="<instance>" serviceType=<_raop._tcp|_airplay._tcp> derivedID=<colon-hex> txtDeviceID=<deviceid|none>`. Logging is PURE ADDITIVE (no behavior changes); uses existing `FileHandle.standardError.write()` convention with distinct `.DIAG` prefix for easy grep. — `swift build` clean; `swift test --filter NativeDiscoveryTests` 41/41 0 failures (diagnostic output visible in test logs); window-harness 48/48 PASS, popover-harness smoke test ok; log format example: `[Audiouter] NativeDiscovery.DIAG: resolved name="6B2E52B73717@Dev Speaker" serviceType=raop derivedID=6B:2E:52:B7:37:17 txtDeviceID=(none)`. Future T3 task will remove or convert logging once root cause is found.
T1 — ported the multi-stream stream_id surgery from airplay.c into the RAOP sender (raop.c): stream_id on raop_master_session/raop_session, keyed master_session_make reuse on (stream_id, quality, encrypt), gated raop_write fan-out on obuf stream_id == master stream_id, added raop_test_* seam; fixes per-app routing + pitch-up/crackle on AirPlay-1; +4 RaopMultiStreamMasterSessionTests, swift build clean, swift test 136/136 green; VENDORED-DIFFS Entry 3b.

- T5 (research-only, no code changes): investigated recurring "time ping lost" RAOP warnings; concluded event-loop starvation from audio-write load, not a bug in timing_svc_cb; recommend re-test after T1 before scoping a fix.
- [T4a] Research-only pass (no code changes): investigated the per-app-routing rebind vs AirPlay-1 (RAOP/shairport) sync-break + "interruption not allowed" rejection; traced raop_device_stop/session_teardown(_cb) (real RTSP TEARDOWN ack, not optimistic), AirPlayEngine.removeOutput/startOp (genuinely awaits that completion), and NativeBackend.performBindOp/.rebind (globally-serialized remove→add). Delivered plain-language A/B/C recommendation to Alec.
- [T8-raop-mute] Made AP1 mute send true silence (-144 dB) instead of -30 dB: modified NativeBackend.setMuted to detect AP1 devices (supportsAirPlay2==false) and pass sentinel value -1.0 to engine instead of 0.0; modified AirPlayEngine.applyVolumeOnDevice to detect negative normalized values and pass through as -1 to C device.volume, which raop_volume_from_pct interprets as out-of-range and maps to -144 dB true mute; AP2 behavior unchanged (continues using 0.0 with stashed-volume restore); unmute path uses normal volume restoration via restoreEffectiveVolume. `swift build` clean; `swift test` 136/136 AirPlayEngine + 769/769 AudiouterCore 0 failures; AP2 mute behavior verified unchanged by existing tests.
[T4b] Excluded AirPlay-1-only devices from per-app routing: PopoverController.availableAirPlayDestinations (AudiouterCore/Sources/AudiouterPopoverUI/PopoverController.swift) now filters out `!supportsAirPlay2` devices from the destination menu (an existing route-fallback already reverts a now-invalid target to Current Device), and NativeBackend.handleDestinationSetsChanged (Sources/AudiouterCore/NativeBackend.swift) adds a defensive guard skipping bind/rebind ops for AP1 device IDs as a stale-state safety net; new tests testAppRowDestinationMenuExcludesAirPlay1OnlyDevices (PopoverControllerTests) + testAppRouteToAirPlay1OnlyDeviceIsRefused (NativeBackendTests); swift build clean, swift test 771/771 (2 skipped) 0 failures.

- [AP1-volume-cliff] RESEARCH-ONLY (no code changes): root-caused the AirPlay-1 (RAOP/shairport) "silent below ~50%, audible above" volume cliff. Verified `device->max_volume==11` survives to volume-set time (struct passed by ownership through player_device_add→outputs_device_add, never copied), so raop_volume_from_pct sends a CORRECT linear -30..0 dB value (50%=-15 dB); ruled out the AP2 max_volume=0 bug and any device-volume PCM gain. Cliff is receiver-side: shairport-sync (default software volume, full mixer range) stretches AirPlay -30..0 across its ~100 dB mixer, rendering the lower ~half inaudible. Recommended fix = AP1-gated perceptual/compressed volume CURVE in the Swift layer (NativeBackend), NOT vendored raop.c, plus a DPRINTF in raop_set_volume_internal to confirm on-wire dB in the next live test. Full findings in agent report.
- [meters-dead-live] RESEARCH-ONLY (no code changes): investigated live report of BOTH the AP1 device meter and Main Out meter reading zero during real playback. Traced the whole metering pipeline (PopoverController popoverDidShow/Close → onMeteringActiveChange → NativeBackend.setMeteringActive → NativeCaptureCoordinator/AppRouteMixer/LocalPlaybackEngine → emitLevel/emitCombinedLevel → BackendEvent.level → AppDelegate → PopoverController.updateLevel/dispatchLevel → DeviceRowView/MainOutRowView.setLevel); none of it is touched by this branch's uncommitted AP1 diff, and all 94 NativeBackendTests (incl. metering-specific ones) pass — no code regression found. Prime suspect: no test exercises a REAL NSPopover's popoverDidShow/popoverDidClose firing (all metering tests bypass it via test_isShownOverride/test_pushLevel), so a live-only activation gap is undetectable by the green suite. Recommended next step: rerun with the already-wired `AIRPLAY_DEBUG_LEVELS=1` env var (AppDelegate.swift ~line 132/645) to see whether `.level` events reach AppDelegate at all. Full findings + ranked hypotheses in agent report.
- [VOL-AP1] AirPlay-1 volume "cliff at ~50%" fix: root-caused NOT a code bug (sender sends correct -30..0dB; shairport-sync default stretches that across the receiver's full ~100dB mixer -> bottom half inaudible). Added AP1-gated perceptual curve in Swift (NativeBackend.engineVolumeAP1 + engineVolume(forID:), 4 call sites routed) compressing UI 0-100 onto [airPlay1MinVolumeDB=-12dB, 0]; AP2 linear path untouched. Left raop.c byte-identical except a TEMP DPRINTF logging on-wire dB (marked, remove before ship). +testEngineVolumeAP1KeepsWholeSliderAudible. Floor tunable by ear.
- [METER-DIAG] Dead-meters (device + Main Out) investigation: pipeline reads correct + untouched by AP1 work + all metering tests green -> likely first live test of metering, live-only break. NO blind fix. Added TEMP setMeteringActive() stderr log (env AIRPLAY_DEBUG_LEVELS=1) to confirm activation fires; pair with existing AIRPLAY_DEBUG_LEVELS level-flow log to bisect activation-vs-paint on next live run.
- Both fixes: swift build clean, AudiouterCore 772/2skip/0fail, AirPlayEngine 136/0. NOT launched (per Alec).

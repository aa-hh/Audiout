# Phase 3 Fix Execution — Progress

Started from the finalized, cost-checked, Alec-approved plan. 15 of 35 tasks
deferred (correction from the original 11 — a fuller pre-launch collision
check found DeviceRowView.swift and scripts/make-app.sh also genuinely dirty
in other active worktrees, and found several onboarding/AppDelegate files
that were dirty at checkpoint time are clean again now — likely committed by
the PTP-helper session in between). Deferred tasks: G-BUG, HEADER-NOOP,
MUTE-FEEDBACK, CONNECT-VOLUME, NATIVE-DIAG, EMPTY-NET, POPOVER-COPY, PICKER,
A11Y-DEVICEROW, HOUSEKEEPING, ICON-BRANCH, RELEASE-CONFIG, G2-NOTARIZE,
PERF-CODEC, G2-LIVE-VERIFY.

Each task appends one line here when it lands: `- [id] status — one line`.

---
- [DESIGN-FEATURE-INTRO] done — wrote docs/plans/phase-3-findings/proposals/feature-intro.md proposing 3 options (onboarding tour, contextual popover hints, in-app How-It-Works page) for teaching app capabilities, not just permissions; recommends contextual hints first + reference page, forced tour only if full-coverage guarantee is required.
- [DESIGN-CARDS] done — wrote docs/plans/phase-3-findings/proposals/cards.md proposing 3 options for the Devices/Applications card confusion (G1-N3); recommends Option B, tagging redirected devices right on their Devices-card row so the relationship between "Selected" membership and per-app redirects is visible where it happens, not just better-labeled.
- [DESIGN-LOCALMIX] done — wrote docs/plans/phase-3-findings/proposals/local-mix.md on the Current-Device-plus-AirPlay mix restriction (G1-N6); the engine's `localOutput`/`setLocalOutputEnabled` API is a confirmed 0%-implemented placeholder (`isImplemented` hard-coded false, unwired into NativeBackend), so recommends Option B (surface the existing refusal reason visibly in the UI instead of tooltip-only) now, with the real synced-local-output build (Option A) treated as a separate scoped project later.
- [DESIGN-SETTINGS] done — wrote docs/plans/phase-3-findings/proposals/settings.md brainstorming ~11 new paid-tier settings across General/Audio/Appearance/Advanced (grounded in today's 4-control Settings surface + G1-N15/N16 and other audit findings); top picks for pre-launch: About & Support, Check for updates, Resume previous speakers on launch, Automatically reconnect a dropped speaker, global keyboard shortcut, Restore Default Settings.
- [R1] no-bug-found — LevelMeterView's redrawFill has no floor (fillLayer.frame height = bounds.height * displayed, proven 0.0pt via live debug instrumentation for every 0-target row incl. homepod-bed); C2's "green sliver" is an artifact of popover-snapshot's bitmapImageRepForCachingDisplay/cacheDisplay offscreen-capture technique, not a live-app bug — matches the owner's real-usage report; visual.md's C2 finding should be treated as false/artifact, not fixed in LevelMeterView.swift.
- [R2] done — audit was RIGHT: MixerWindowController unconditionally called setContentSize+center() after setFrameAutosaveName, discarding any restored frame on every construction; invisible to the live smoke test because AppDelegate builds the controller once per process and reuses it (same-session close/reopen never re-ran init()). Fixed via setFrameUsingName (trustworthy restore-and-report) gating the default-size/center() fallback, keeping setFrameAutosaveName only to arm future autosave. Also found setFrameAutosaveName's own Bool return is a red herring (always true, even for a never-saved name) — verified empirically with a standalone AppKit script.
- [SHIM] done — wrapped 5 AVAudioEngine call sites (connect x2, play x2, scheduleBuffer) in LocalPlaybackEngine.swift with catchingObjCException, rollback mirrors existing engineNotRunning soft-fail pattern; build clean, 771 tests green; live crash-repro deferred to signed-build session
- [DIAG-PANEL] done — PopoverPanelViewController.insertRow searched CardView.contentStack for the failed device's sibling row, but device rows live in bodyStack (one level deeper), so the lookup always missed and the "Couldn't connect" panel never attached; fixed by inserting into sibling.superview directly, added the same Reduce Motion gate setCardCollapsed uses, and added a view-tree attachment regression test (proven to fail pre-fix); 785 tests green, connection-states snapshot now shows the panel under the failed row.
- [A11Y-LABELS] done — theme tiles now announce selected state (.radioButton + AX value), icon-grid cells get plain-language VoiceOver labels instead of raw SF Symbol names, GroupRowView's claimed .button role now actually responds to accessibilityPerformPress, AppRowView's composed label folds in routing destination + "not running" status; commit 2b497f5, 13 new/extended tests green (full-suite run showed unrelated pre-existing failures from other agents' concurrent uncommitted edits in this shared worktree — not touched).
- [A11Y-GROUPS] done — DeviceIconWellView now keyboard/VoiceOver-operable (acceptsFirstResponder, Space/Return/keypad-Enter in keyDown, accessibilityPerformPress, system focus ring); Tab-traversal finding: nothing in the window's lifecycle ever called NSWindow.makeFirstResponder (even programmatic auto-select doesn't promote first responder, only a real click does), so a freshly-shown window had no key view for Tab to advance from — fixed the "nothing tabbable from launch" root cause via SidebarViewController.viewDidAppear() seeding the outline view as first responder + recalculateKeyViewLoop(); NOT fixed (needs-follow-up, MixerWindowController.swift out of scope for this task): re-seeding after the content pane swaps (editor/detail/empty) may still need its own recalculateKeyViewLoop() call — untested live. Commit e3f5332, 44 new/extended tests + 48/48 window-harness green (full-suite run again showed unrelated concurrent-agent breakage in AudiouterSettingsUI/AboutView — not touched, confirmed transient).
- [ABOUT] done — added Settings › General › "About Audiouter…" opening a standalone AboutWindowController (kept out of the single-screen General pane itself — inlining it measured ~1039pt, breaking SettingsWindowControllerTests' 750pt sizing-regression guard); shows bundle-read version/build (AboutInfo.current(bundle:), never hardcoded), GPL-2.0-or-later + TODO-marked placeholder source URL, NOTICE's per-component third-party credits in a scrollable text view, TODO-marked support contact; fixed a dark-mode illegible-text bug (missing NSVisualEffectView background, same class of bug as SettingsRootViewController's own documented fix) caught via a manual offscreen light/dark render before committing; commit 75426c6, 11 new tests + full 806-test suite green.
- [COPY-SETTINGS] done — updated Settings UI labels: "Run Setup Again…" → "Check Permissions…", shortened & clarified exclusion subtitle, "Restore Mac audio after wake if speakers don't return" → "Restore Mac audio if speakers don't reconnect" (no truncation), "Add application…" → "Add app…", "Apply" → "Apply Settings" (idle state). Updated 6 source files + comments, 2 test assertions, regenerated settings snapshots; build clean, 836 tests + 2 skipped green.
- [ONBOARD-GATE] done — confirmed the original bug still existed post-PTP-merge (its revocation audit only catches a permission that regressed after setup completed, and deliberately treats untouched .unknown/.notRegistered as fine, so a fresh Done-click with nothing granted still finished silently); added SetupModel.requiredPermissionsNotGranted() + an NSAlert confirmation on Done when any required permission (audio/network/PTP helper — Remote Control stays optional) isn't actually granted, Continue Anyway still finishes (guidance not a gate); 7 new VC tests + 5 new model tests, 849 tests green, onboarding snapshots regenerated (no pixel diff, layout untouched).
- [CONTROL-PANEL-SHIP] done — made the AIRPLAY_CONTROL_PANEL shell shippable (kept flag-gated per RELEASE-CONFIG): (1) native close button now VISIBLE (miniaturize/zoom hidden) so there's a real way out; (2) status-item click during a live session now TOGGLES the shell — closes it if showing (real close → lands home on popover), restores it if tucked away — instead of only re-fronting; (3) Escape wired deterministically via new ControlPanelPanel.cancelOperation→performClose; (4) fixed the dark-mode half-render (C3b) — the hosted content is now left transparent (was a frozen windowBackgroundColor.cgColor snapshot) so it shows the backing bubble's LIVE windowBackgroundColor, making Groups' content pane match its sidebar and its standalone window; added ControlPanelBackingView.viewDidChangeEffectiveAppearance for mid-flip repaint; (5) 4 new shell tests (close button visible, Escape closes, performClose lands home, isPanelVisible wiring). Touched only ControlPanelWindowController/ControlPanelBackingView/AppDelegate + 2 AGENTS.md + tests (no Mixer/Settings controller edits). swift build clean, 853 tests + 2 skipped green, window-harness 48/48, panel-chrome-5 snapshot regenerated (dark now consistent). Commit on branch.
- [ONBOARD-TONE] done — confirmed the bug still existed post-PTP-merge: SetupModel.refreshStatuses() (called from OnboardingWindowController.appDidBecomeActive on EVERY plain app reactivation while onboarding is open, not just explicit gestures) still called the audible AudioCapturePermissionProbing.probe() whenever audioStatus was .denied. The merge's new silent read (currentStatusSilently()) was wired only into AppDelegate's post-completion revocation watch, which explicitly skips while onboarding is open — so a bare Cmd+Tab away/back during onboarding still replayed the tone. Fixed by switching refreshStatuses()'s System Audio branch to the silent read (nil result leaves audioStatus unchanged, matching auditRequiredPermissions()'s posture); requestAudioCapture() (the explicit "Allow…" tap) untouched. Commit 8bcfede, 2 new regression tests, 46/46 SetupModelTests + full 855-test suite green (one NativeBackendTests flake under concurrent full-suite load, passed clean in isolation, unrelated — out of scope file).
- [ONBOARD-ICON] done — replaced generic airplayaudio SF Symbol in onboarding header with app's real icon; fetches via NSApp.applicationIconImage so it tracks deployed icons without stale asset references. Build clean, 769 tests green, onboarding-snapshot light/dark regenerated.
- [S4+S5] done — merged spine stage: (S4) blocked local-Mac row's refusal note is now reachable from EVERYWHERE on the row (body/node via mouseDown already in tree; NAME click now routes to deviceRowDidRequestBlockedExplanation instead of no-op), blocked rows keep NORMAL text (dropped the V12 tertiary name-grey so blocked ≠ unavailable, R5), and the row's VoiceOver hint (accessibilityHelp) speaks GroupController.localMixRefusalReason; (S5) FINAL §3.4/§4.7 dormant semantics replace the transitional ones — derived-equal (checked set == active group's members) renders full emphasis with NO "Inactive" note, genuine divergence posts the note and tints ONLY rows outside the active target (bus-node tint, checkbox at full alpha), bus origin stub dims only under divergence (new MainOutRowView.apply busOriginDimmed: param, host-resolved), failed member full-emphasis exemption asserted through the popover path incl. never-edits-saved-group; note reconciles LIVE off membership toggles + failure auto-deselects via refreshDeviceRowsReconcilingCardNote(). Rewrote testDormantDevicesCardNoteAndDimming to the new semantics + added failed-member, blocked-note-toggle, blocked-name-click, hint, and blocked-vs-unavailable tests. swift build clean, full suite 966 tests green (2 skipped live tests), popover-harness 84/84, all three snapshot modes byte-identical across double runs (goldens NOT overwritten — Mac-row name brightening means the gate re-goldens).
- [R45] done — reworked the shipped-wrong T4/T5 per Alec's CORRECTIONS: (1) MainOutRowView's master meter moved OUT of its floating below-icon position and INTO identityStack as an arranged subview after nameLabel, mirroring DeviceRowView's under-name meter exactly (same position/geometry, thicker via LevelMeterView(thickness: masterMeterThickness) 6pt vs device 3pt); removed the now-dead masterMeterBottomInset constant and the meter's own leading/bottom anchors (the stack positions it). (2) BusRailOverlayView's rail origin no longer draws a horizontal hook + junction-into-meter dot; busOriginView (MainOutRowView) is repositioned to the SAME railGutterCenterX column device rows use for their own bus node (busColumnWidth × row height, centered), and railHookAnchor now reads busOriginView's frame instead of meterView's — the overlay draws a small clean busOriginDotDiameter dot there (renamed from masterMeterJunctionDotDiameter) with the horizontal jog kept only as a defensive no-op for a future non-matching anchor. (3) PopoverColumnGrid.busNodeDiameterSelected 13→15pt, busNodeDiameterUnselected 9.5→11pt — MembershipBusView.nodeRadius/BusRailOverlayView's gap+detour-arc math already derive off these constants dynamically, confirmed clean at the new sizes with no further code changes needed. Files: AudiouterCore/Sources/AudiouterPopoverUI/MainOutRowView.swift, AudiouterCore/Sources/AudiouterSharedUI/{BusRailOverlayView,PopoverColumnGrid}.swift. swift build clean; swift test --parallel exit 0, all 1069 tests green. Rendered default/rail-depth/connection-states popover-snapshot modes (light+dark) to scratch and visually confirmed: master meter thicker gold bar directly under "Main Audio", origin dot sits cleanly in the left gutter never touching the icon or meter, filled/selected nodes visibly bigger than hollow ones. Did NOT touch DeviceRowView.swift or MembershipBusView.swift (no changes needed — MembershipBusView's docstrings already carried no hardcoded pt literals to update). NOTE FOR T9 (energize): the rail's origin anchor is now `MainOutRowView.busOriginView` (railGutterCenterX column, row-height tall, centered vertically) via `railHookAnchor(in:)` — NOT the meter. Node/rail states (`.member/.connecting/.pending/.failed/.nonMember/.blocked/.origin`) are unchanged in vocabulary/semantics, only their drawn sizes (busNodeDiameterSelected/Unselected) changed; energize's pending/connecting/fill sequencing can key off the same node kinds and origin anchor as before, just with the corrected geometry.
- [FADER] done — the missing Warm Signal fader skin (spec §5 "slider track/knob look"): new WarmFaderCell (AudiouterSharedUI), a drawing-only NSSliderCell subclass overriding ONLY drawBar/drawKnob, installed on the DeviceRowView / MainOutRowView / AppRowView volume sliders (cell swap first in buildSubviews so min/max/target/action land on the new cell; tracking/keyboard/scroll-wheel/isContinuous/VoiceOver/focus ring all stock — GroupRowView + Settings sliders deliberately untouched). Draws: flat recessed `well` trough (~4pt, hairline rim + drawn inner top shade, no layers), filled portion in the ember→gold gradient ONLY while route-armed ∧ enabled — the exact §3.3 predicate the RouteArmedDotView renders (AppRowView: routed ∧ running, spec §5.1) — neutral warm `ringConnected` fill otherwise (dimmed at faderDisabledAlpha when disabled), and a ~9×15pt rounded-rect `raised` thumb with hairline outline + 1px top highlight replacing the stock circle. All colors via Tokens at draw time → light/dark + Increase Contrast + accent dial (systemAccent ⇒ engaged fill = controlAccentColor) for free; byte-deterministic under cacheDisplay (asserted). New geometry constants in PopoverColumnGrid (faderTrackHeight/ThumbWidth/ThumbHeight/CornerRadii/DisabledAlpha). 16 new WarmFaderCellTests (skin installed ×3 rows, engaged-fill truth-table edges per row, target/action dispatch survives the cell swap via new test_fireSliderAction — real AppKit dispatch, not the delegate shortcut — config survives, double-render byte-identical both appearances); per-row test_isFaderEngaged/test_hasWarmFaderSkin hooks. swift build clean, full suite green (1036 tests; one known NativeBackendTests concurrency flake passed on re-run + in isolation), popover-harness 84/84, ALL five popover snapshot modes regenerated + double-run byte-identical; settings/window/onboarding snapshots verified pixel-identical (no re-golden needed). SharedUI AGENTS.md updated (rule + map).
- [T7] done — v4.1 CORRECTIONS "FEED needs the colour chip, not just tinted text": new FeedChip (AudiouterSharedUI) draws a small derived-colour rounded-square NSTextAttachment (feedChipSize 5pt/feedChipGap 2pt/feedChipCornerRadius 1pt, new PopoverColumnGrid constants — kept deliberately tight, see below) resolved live from an AppTetherColor NSColor so it tracks light/dark + Increase Contrast for free. DeviceRowView.appSegmentColor(for:) (the T6 seam, was flat secondaryLabel) now reads a host-supplied `appTintColors: [String: NSColor]` map (falls back to AppTetherColor.neutralFallback if unmapped) and prefixes every APP feed segment with the chip; the neutral main-mix "System"/group segment and the failure-red override never get one. AppRowView.Configuration gained `tetherColor: NSColor?` (default nil, zero behavior change for existing callers) — when set it prefixes the SAME chip onto the app's own name in the App Exceptions row, additive to (never replacing) the existing liveness-driven name-color logic. PopoverController computes tints via new appTintColor(for bundleID:)/appTintColorsByName() (AppTetherColor.color(forBundleID:icon:), which already caches per bundle id) — wired into both applySelectionState device-row apply() calls and makeAppRow (tetherColor only when route.destination is an actual .device, not "Current Device"/"No Redirect"). GOTCHA hit + fixed: NSTextAttachment inserts one \u{FFFC} object-replacement char into attributedStringValue.string — test_feedText/test_nameDisplayText/test_nameTextColor now strip/skip it so every pre-existing plain-text test kept passing unchanged; also had to shrink the chip's footprint hard (would've been 9pt/5pt/2pt matching the html reference) because "System · Music · Safari" was already only ~17pt under feedColumnWidth's 136pt budget — a chip any bigger overflowed two PRE-EXISTING pinned tests (testManualMemberPlusTwoApps + a new one), so did NOT touch feedColumnWidth itself (shared/hot constant, left for a future task if a wider chip is wanted). Renamed/rewrote FeedColumnTests.testAppSegmentColorIsFlatSecondaryLabelToday → testAppSegmentColorUsesTheHostSuppliedTetherTintNotFlatSecondaryLabel + 4 new FeedColumnTests (fallback, chip-count present on app segments/absent on main-mix/error) + 4 new AppRowViewTests (chip present/absent, additive to idle suffix + liveness color, unaffected-by-host-omission back-compat). swift build clean; swift test --parallel 0 failures, full 1085-test suite green (26 FeedColumnTests + 4 new AppRowViewTests all pass). Rendered default + feed-composite popover-snapshot modes (light+dark) to scratch and visually confirmed: Office's FEED reads "System · [chip]Music" with the identical chip colour on the App Exceptions "Music" row; Safari (unrouted) carries no chip; halo rings and meters stay untinted gold/neutral (guard item 6 intact). Did NOT touch DeviceRowView/MainOutRowView drawing beyond the FEED segment seam, and did not regenerate popover-harness goldens (T11's job) — no golden PNGs were overwritten. NOTE FOR DOWNSTREAM: PopoverColumnGrid gained feedChipSize/feedChipGap/feedChipCornerRadius (new file FeedChip.swift, AudiouterSharedUI) — if a later task widens feedColumnWidth, the chip size can grow back toward the 9pt/5pt/2pt reference proportions.
- [T8] done — item 8 "muted → brighten on device connect": DeviceRowView now tracks `controlsMuted` (was a local-only `let` in `apply`, promoted to a stored property) and `previousConnectionState`, and fires a one-shot `brightenOnConnect()` cross-fade on the exact connecting/reconnecting → connected edge (never on failure, never on the row's own first apply, never off-screen). Also closed the actual gap item 8 named but Call 1 never built: the FEED composite's text (neutral "System"/group segment + app-tint segments + the separator/AP1-tag chrome) now dims to `tertiaryLabel`/half-alpha tint while `controlsMuted`, mirroring the fader/readout dimming Call 1 already had. GOTCHA (cost the most time): `CALayer.add(_:forKey:)` for a `CATransition` ALWAYS files it under the literal reserved key `"transition"` — a custom `forKey` string is silently ignored by AppKit/QuartzCore (verified with a 6-line throwaway CALayer script), so `brightenTransitionKey` had to become that reserved string, not a project-specific one, or `animation(forKey:)`/`removeAnimation(forKey:)` would never find what was just added. Added a row-level Reduce-Motion seam (`test_reduceMotionOverride` + its own `NSWorkspace.accessibilityDisplayOptionsDidChangeNotification` observer, mirroring RouteArmedDotView) since this is the first DeviceRowView-level (not sub-instrument-level) animation. New test file DeviceRowConnectBrightenTests.swift (10 tests): connect-edge brighten fires/doesn't (reconnecting too), Reduce Motion snaps instead of animating, a mid-flight Reduce-Motion toggle cancels it live, no brighten on first-apply or off-screen or on a failure edge (either direction), FEED dimming asserted via new `test_feedNeutralColor` hook. swift build clean; swift test --parallel exit 0, all 1087 tests green. Self-checked via a throwaway (reverted, not committed) popover-snapshot mode rendering three DeviceRowView instances side by side — connecting (dashed ring/dim node/dim fader/dim "System") vs connected-no-Reduce-Motion vs connected-with-Reduce-Motion (the latter two pixel-identical, confirming Reduce Motion only removes the transition, never the resolved state). Did not touch PopoverController.swift — every real device-row apply() already flows through PopoverController's existing in-place `row.apply(...)` refresh path (confirmed via `applySelectionState`/`refreshDeviceRows`), so the edge-detection living inside DeviceRowView.apply() alone is sufficient; no host-side wiring was needed.
- [T10] done — item 6 "no tint on rings or meters (guard)": new NoTintOnRingsOrMetersGuardTests.swift (AudiouterCoreTests) enforces via 13 new IsolatedTestCase assertions that tether color (app-derived AppTetherColor hues) never reaches halo rings or meters — rings stay connection-only (ringConnected/failure), meters stay warm-only (ember/gold/caution), tether tints appear ONLY on FEED column app-name segments and App Exceptions chips (the allowed seams). Tests verify LevelMeterView gradient foundation, device/master-out meters inherit warm gradient, HaloRingView stroke stays connection-state (never tether-tinted even with multi-app redirects), MembershipBusView.node stays enum-only (never color-typed), all edge cases (muted + redirect, connecting + redirect). Guard intentionally touches zero production code — it verifies test hooks are correctly wired so bad futures fail here first. swift build clean; swift test --parallel 0 failures, all 1100 tests green. Guard structure mirrors RouteArmedSignalTests' color-comparison idiom (assertSameHue via sRGB components, 0.01 accuracy). No snapshots touched (guard is test-only).
- [T9] done — item 9 energize source-switch sequence, drawing/wiring-only (no membership/connection/routing model change). New DeviceRowView.apply(energizePending:) presentation flag: an `.off` member of a Main-Audio switch renders MembershipBusView `.pending` (ember dashed, on-spine) the instant the switch fires, hands off to the live `.connecting`→`.member` node as connectionState advances (the `.off` guard means the model node always supersedes), and is REMOVED under Reduce Motion (snaps to resolved; accessibilityDisplayOptionsDidChange re-runs updateBus so a mid-flight toggle drops it live). VoiceOver equivalent: a pending row speaks "connecting" via accessibilityStateSuffix. BusRailOverlayView now dims a `.failed` in-spine segment (item 4) — inert for existing goldens (no controller path puts a failed node in-spine; a failed member auto-deselects → nonMember). PopoverController orchestration: mainOutRow(_:didSelect:) → beginEnergize (posts the VoiceOver transition announcement always; seeds energizePendingIDs = selected `.off` members unless Reduce Motion), refreshDeviceRows → reconcileEnergize prunes the beat as members leave `.off` + fires the one-shot settle announcement when the target stops moving; NO timers (top-to-bottom fill rides the live connectionState progression + item-8 per-row brighten). swift build clean; swift test --parallel exit 0, all 1110 tests green incl. 10 new EnergizeTests (IsolatedTestCase). Rendered energize-mid + energize-reduce-motion (light+dark) to scratch, byte-identical on double-run, visually confirmed: mid = filled-gold member + gold-dashed connecting + 2 ember pending + red failed w/ diagnosis; reduce-motion = solid gold spine, would-be-pending members snapped to filled member nodes, no dashed residue. Did NOT overwrite committed goldens. NEW SNAPSHOT MODES FOR T11 TO GOLDEN: `energize-mid-sequence` (popover-energize-mid-{light,dark}.png) + `energize-reduce-motion-static` (popover-energize-reduce-motion-{light,dark}.png).
- [T11] done — golden regeneration sweep + determinism audit: swift build clean, swift test --parallel 0 failures (1110 tests); rebuilt popover-snapshot and regenerated all 9 modes/20 PNGs into dev/notes/popover-snapshots (default, connection-states, live-routing, dormant-group, local-mix-blocked, rail-depth, feed-composite, energize-mid-sequence, energize-reduce-motion-static incl. the 4 new energize fixtures); re-rendered a second pass into a scratch dir and `cmp` confirmed byte-identical across all 20 files; `git diff --stat` on goldens shows the 16 pre-existing PNGs changed (expected: master-out meter under Main Audio thicker than device meters, rail gutter origin, larger selected/smaller unselected nodes, FEED composite text + tether-tinted chips, centered fader thumb, muted-to-bright device wiring) plus the 4 new energize PNGs added — no unexpected files or unrelated drift. Visually spot-checked popover-dark.png and popover-energize-mid-dark.png against the corrected layout (meter placement, FEED "System · Music" tint, energize pending/connecting/failed states) — matches dev/notes/warm-signal-v4.1-polish.md CORRECTIONS block.
- [SETTINGS-TABS] done — Warm Signal screens follow-up item 2 (`dev/notes/warm-signal-screens-followup.md` § Settings), both halves. **Tabs:** `SettingsWindowController` is now an ordinary titled standalone window whose content is an `NSTabViewController` (General/Appearance/Audio, `tabStyle = .toolbar` + `window.toolbarStyle = .preference`, SF Symbols, stock chrome only — no Tokens/gold per warm-signal-v3.md:468-474), always opening on General, non-resizable, position remembered. Supersedes the 2026-07-17 "One screen, not tabs" decision (owner lock 2026-07-22); the AGENTS.md rule was rewritten rather than deleted, keeping and STRENGTHENING its sizing-bug trap. Four probe-confirmed AppKit traps handled: tabs are all mounted before `NSWindow(contentViewController:)` (an empty tab controller locks a 500×500 fallback nothing un-sticks); `NSTabViewController` never resizes its window on tab switch; its own `view.fittingSize` reads (0,0) off any tab but the first, so sizing measures the SELECTED CHILD; and `setFrameAutosaveName`'s Bool return does not mean "restored", so `setFrameUsingName` → autosave → `center()`-only-if-none is used (same fix shape as MixerWindowController.swift:204-222). Two plan corrections found by probe: `preferredContentSizeDidChange(for:)` is NEVER called for a tab item's view controller (a selected pane republishing at 600pt left the window at 452pt) — runtime pane growth is driven by KVO on each pane's `preferredContentSize` instead; and measuring the child's `fittingSize` feeds back on itself once shown (Audio reads 561×205 embedded vs 460×221 fresh, wider AND shorter), so width is pinned to `SettingsForm.contentWidth` and height taken from the published `preferredContentSize`. Measured result: General 460×192 / Appearance 460×271 / Audio 460×452, `frame.maxY` constant (title bar anchored, window grows downward). **Decoupling:** with Settings unhosted, the shell's cross-surface size stomping is structurally impossible — `AppDelegate.openSettings` lost its `useControlPanel` branch and the provably-unread `ControlPanelSurface`/`activePanelSurface` state was deleted. `ControlPanelWindowController.setContent` gained `defaultSize:` applied ONLY on first mount of a given content (tracked by `ObjectIdentifier`) so a size the user drags survives a re-host for the process lifetime; the shell deliberately does NOT adopt the Settings window's re-measure-before-every-show rule (documented in AGENTS.md, since "fixing" the inconsistency would break that). Two real bugs found while there: assigning `window.contentViewController` is NOT size-neutral (AppKit resets to a generic 500×500 first, which would defeat a naive skip-on-remount guard), and `addChildWindow` tracks translation but NEVER size, so the decorative bubble/beak/shadow desynced on any user resize — fixed via `windowDidResize`. Also added File ▸ Close (⌘W) to `installMainMenu`, which had only an App menu; without it AppKit synthesizes no ⌘W for the new standalone window. **Goldens:** settings snapshots go 2 → 6 (3 tabs × light/dark); `settings-snapshot` renders each pane constraint-pinned at `contentWidth` and resolves wrapping labels' `preferredMaxLayoutWidth` BEFORE measuring — without that the Audio pane measures 561pt (the "Connects at N% …" hint demands 521pt on one unbroken line) and renders clipped. With `.toolbar` style the tab bar is window chrome, so it is deliberately absent from these pane renders. `mixer-5-panel-chrome-{light,dark}` regoldened 720×518 → 720×479 (= 460 + beak): the OLD golden encoded the size-leak bug, having inherited the device-detail window's 499pt height. NOTE: the other 13 mixer PNGs also differ from their committed bytes at unchanged dimensions — verified deterministic run-to-run and PRE-EXISTING (goldens last regenerated 2026-07-22 at 24f18c7; AudiouterWindowUI/SharedUI changed 07-24 and 07-25, incl. c147cb7 FEED pill overflow), so they were deliberately left untouched here rather than bundling unrelated drift into this batch — they still need a regolden by whoever owns that work. swift build clean; `swift test --parallel --num-workers 4` exit 0, all 1270 tests green (the 4-worker cap matters — uncapped, PopoverControllerTests/testDeviceRemovedWhileFailedTearsDownPanel times out under contention, passes in isolation); Guard 2 (agents-md-symbol-check) clean; settings + window snapshots byte-identical across double runs. OWED: live verification on a real bundle — ⌘W actually firing in this Dock-less `.accessory` app, the toolbar tab bar in both appearances (window chrome, so no golden covers it), and dragging the Groups bubble to confirm the beak stays put.
- [T4] done — threaded PTPHelperStatus through snapshot scenes; initial scene renders Speaker Sync un-lit (.requiresApproval)
- [T1] done — added 4 dial-aware permission-row Tokens.Color cases (permissionSystemAudio/permissionLocalNetwork/permissionRemoteControl/permissionSpeakerSync) per Q1/Q2/Q3/Q5/NEW-1: warmed+deepened from systemBlue/systemIndigo/systemPurple (Speaker Sync moves into the gold family instead of a warmed teal, hue ~21-23° kept strictly below the 28° gold/amber reserved band). New resolver permissionDynamic(name:full:subtle:) alongside warmDynamic/accentDynamic — deliberately NOT accentDynamic (doc comment explains why: its .systemAccent branch would collapse all 4 identity hues onto the same controlAccentColor-scaled value, and its subtle:nil->.clear fallback would render an invisible glyph); .fullGold and .systemAccent both resolve the FULL column, .subtle resolves the authored SUBTLE column. All 32 authored hexes (4 tokens x 2 dial columns x 4 appearance/IC variants) hand-verified >=3:1 vs BOTH Tokens.Color.panel and Tokens.Color.raised in their own theme (WCAG relative-luminance formula, cross-checked against AppTetherColorTests' reference implementation) and clear of both AppTetherColor.ReservedBand windows (gold/amber [28,68), failure-red [0,12)u[350,360)); mutual hue separation >=45° in every column. Subtle column authored by hand, not derived (a mechanical desaturation of Speaker Sync's dark hue undershot the floor at ~2.8:1 before hand-raising). swift build clean. Only file touched: AudiouterCore/Sources/AudiouterSharedUI/Tokens.swift.
- [T2] done — threaded the per-permission colour onto IconTileView's resting glyph (Q1-Q3): IconTileView gained a `color` init param (default Tokens.Color.secondaryLabel) applied to restingImage.contentTintColor only — tile fill/rim in updateLayer untouched (Q3). PermissionRowContent gained an `iconColor: NSColor` field, threaded from OnboardingViewController's three PermissionRowContent call sites (System Audio -> Tokens.Color.permissionSystemAudio, Local Network -> permissionLocalNetwork, Remote Control -> permissionRemoteControl); PTPHelperRowView's IconTileView call site passes Tokens.Color.permissionSpeakerSync directly (no PermissionRowContent there). setLit's gold crossfade (Q2) is untouched -- it still always ends at Tokens.Color.gold regardless of resting tint, and its Reduce-Motion/off-window guards are unmodified. Fixed 3 stale doc comments: PermissionRowContent's + PermissionRowView's class doc (both said tiles share one warm-neutral tint with per-row colour 'retired'), IconTileView's class doc (same claim) and IconTileView.side's doc (falsely claimed a hero uses a larger explicit size -- git grep IconTileView shows both call sites use the default). Added 3 test_ hooks on IconTileView: test_restingTint, test_litTint, test_fillColor (for T3). swift build clean; swift test --filter OnboardingUITests: 28/28 passed; git grep -n "retired|hero uses a larger" AudiouterCore/Sources/AudiouterOnboardingUI/ returns nothing.
- [T7] done — added AudiouterOnboardingUI/AGENTS.md (296 words, symbol check green).
- [T6] done — superseded-by pointer blocks in warm-signal-v3.md §1.3/§5.8 (0 deletions) + full implementation-record subsection in warm-signal-screens-followup.md § Onboarding.
- [T5] done — regenerated six onboarding reference images; determinism verified; all colours distinct and legible; initial scene shows Speaker Sync un-approved
- [T3] done — new OnboardingPermissionColorTests.swift (6 tests, subclasses IsolatedTestCase): distinctness of the 4 permission tokens in .fullGold and .subtle (both appearances); >=3:1 WCAG contrast vs BOTH panel/raised across 2 dial columns x 2 appearances (32-point sweep, ported relativeLuminance/contrastRatio from AppTetherColorTests + resolved(_:appearanceName:) from SettingsAccentAndHintsTests, both files left untouched -- re-confirmed testAccentDialNeverRemapsFailureCautionOrRing names failure/caution/ringConnected explicitly so it isn't tripped); .subtle actually changes all 4 from .fullGold (Q5); .systemAccent leaves all 4 at their Full-gold values AND mutually distinct (NEW-1 guard); granting still gold-lights the glyph (isLit/test_litTint==gold) while the tile fill stays Tokens.Color.raised (Q2/Q3) -- exercised via IconTileView.setLit directly since PermissionRowView/PTPHelperRowView keep iconTile private (not reachable even under @testable import), flagged as a judgment call. tearDown restores Tokens.accentStyle=.fullGold. swift test --filter OnboardingPermissionColorTests: 6/6 passed. Guard verified live: temporarily aliased permissionLocalNetwork to permissionSystemAudio in the token list -> 3 tests failed exactly as expected (distinctness x2 + systemAccent guard), reverted -> 6/6 green again. OnboardingUITests (28/28) and SettingsAccentAndHintsTests (13/13) both still pass untouched.
- [WS-ONBOARD-COLOURS] done — Warm Signal screens follow-up, Onboarding: per-permission colours restored, warm-harmonized. Four new Tokens.Color cases (permissionSystemAudio ~210deg warm slate, permissionLocalNetwork ~275deg dusty plum, permissionRemoteControl ~322deg muted mauve, permissionSpeakerSync ~22deg deepened brass) resolved through a NEW permissionDynamic(name:full:subtle:) helper — deliberately NOT accentDynamic, whose .systemAccent branch returns controlAccentColor independent of the token and would collapse all four to one colour in Follow-system mode (reason recorded in the helper's doc comment). 32 authored hexes = 4 tokens x {Full,Subtle} x {dark,darkHC,light,lightHC}; Subtle authored not derived, per the gold/ember precedent. All 64 contrast checks clear >=3:1 vs both panel and raised in own theme; all 32 hues clear the gold/amber [28,68) and failure-red [0,12)u[350,360) reserved bands (Speaker Sync sits at ~22deg, deliberately BELOW the gold band so a resting row can never be mistaken for granted — Alec accepted this deviation from 'stays gold' 2026-07-25). Colour lands on the GLYPH only (Q3): IconTileView gained a color: init param applied to restingImage.contentTintColor; tile fill (raised) + hairline rim byte-for-byte unchanged. Granted still crossfades to gold for all four (Q2), setLit and its Reduce-Motion/off-screen guards untouched. Accent dial mutes all four in .subtle only; .fullGold and .systemAccent both resolve the Full column (NEW-1). New OnboardingPermissionColorTests (IsolatedTestCase, 6 tests) asserts distinctness in fullGold AND subtle, 3:1 floors across both dial columns x both appearances, subtle-actually-changes-all-four, systemAccent-stays-distinct (the executable guard against a future accentDynamic 'simplification'), granted-lights-gold, and tile-fill-unchanged; guard proven to bite by temporarily aliasing two tokens (3 tests failed, reverted, green). Stale docs fixed: IconTileView's 'retired per-row chips' comment and the false 'hero uses a larger explicit size' note on IconTileView.side. Snapshot tool threads PTPHelperStatus so the initial scene renders Speaker Sync un-approved; all 6 reference PNGs regenerated, byte-identical on double-run. warm-signal-v3.md got superseded/amended POINTERS only at 5.8 and 1.3 (git diff --numstat = 16 insertions, 0 deletions — locked prose intact); full 32-hex table + ratios live in warm-signal-screens-followup.md. New AudiouterOnboardingUI/AGENTS.md (296 words, Guard 2 clean). swift build clean; swift test --parallel --num-workers 4 exit 0, 1270 tests, 0 failures. NOT committed — left in the worktree for Alec's live review.
- [TCC-PERMISSION-DETECTION] done — three bugs behind "the app thinks it lost System Audio permission", all traced from live telemetry rather than inference. (1) FALSE ALARM: macOS answers TCC three ways (granted/denied/undetermined) and the app collapsed it to two — currentStatusSilently() mapped undetermined -> CGPreflightScreenCaptureAccess() -> false -> .denied, firing the "permission was turned off" banner. That fallback reads the Screen Recording list, which an audio-only app is never in, so it could ONLY manufacture false denials: all 17 denials in telemetry.jsonl came through it, 0 from the direct read. (2) AUDIO STAYED DEAD: isGranted() (the gate every capture coordinator consults) mapped undetermined -> false, so taps were refused even after a confirmed grant; observed live as gate_check granted=false immediately after a helper proved the grant. Both resume paths were also permanently stranded — reconcileCaptureGate() sets captureRunning before a start() that cannot fail, so later reconciles short-circuit; and updateAppRoutes' captureToStart set-difference is empty on an unchanged table while a permission refusal never matched the .processNotYetAudible retry, marking bundles dead forever. (3) VANISHING SETUP WINDOW: presentSetup() had no reentrancy guard — a second call replaced onboardingWindowController, whose release fired the OLD controller's windowWillClose -> onFinished, which nils the reference to the NEW controller, leaving it unretained so it deallocated and closed. PROVEN BY MEASUREMENT (instrumented signed build, TCCBucketDiagnostic + AUDIOUTER_TCC_DIAG): TCCAccessPreflight is cached for the CALLING PROCESS'S LIFETIME — after the grant the in-process read stayed 2 for 177 ticks over 203s while a freshly spawned helper read 0 in the same second; only a new process ever sees the change. The Darwin notification com.apple.tcc.access.changed DOES fire on a grant but does NOT refresh that cache. Critically a real tap CAPTURED AUDIO in the stale process, so this was always detection, never authorisation — no relaunch is required. This settled three contradictory in-repo claims: AudioCapturePermissionProbe's "stale in-process cache" note was RIGHT; AppRelaunchCommand's "only readable via CGPreflight, cached at launch" was wrong on both counts; a memory note claiming a live read was wrong. ARCHITECTURE: notification/action = TRIGGER, freshly-spawned tcc-probe helper = READER (cache is per-process), one-way latch = what the gate consults. New: tcc-probe executable (bundled+signed like ptp-helper), TCCProbeRunner (async, single-flighted — a burst yields <=2 spawns, no timer), SystemAudioCaptureTCC.combine()/combinedStatus()/recordFreshGrant()/effectiveStatus(), PermissionStateObserver (EVENT-DRIVEN, zero timers) on five triggers: Darwin notification, launch, wake, routing chokepoints (NativeBackend.setOutputSet + updateAppRoutes, hooked there not at the 4 GroupController sites so group activation cannot be missed), popover open. Resume: resumeRefusedAppCaptures() + forceCaptureGateReevaluation(), enqueued onto captureControlQueue only from inside a stateQueue critical section per the documented ordering contract; the force-clear applies ONLY in the wants-capture-ON direction, because a blanket clear would skip the stop() and leave a live muted tap — permanent mute. Scope: per-app routes at launch (honouring the no-auto-stream-at-launch decision), per-app + whole-system on wake. Tests: +7 SystemAudioCaptureTCCTests closing a ZERO-coverage gap on the app's most correctness-sensitive gate (full 3x3 truth table; verified non-vacuous by injecting the original bug — 6 of 7 failed, reverted, green), +9 PermissionStateObserverTests, +10 TCCProbeRunnerTests, +5 NativeBackendTests. swift build clean; swift test --parallel --num-workers 4 exit 0, 1301 tests, 0 failures. RECORDED CONSTRAINTS: this forecloses App Store distribution (no public API exists for this grant; Alec confirmed Developer ID only, knowingly); mid-session revocation is undetected until next launch/wake (latch is one-way by design; not a regression). KNOWN GAPS: AppDelegate has no test target depending on it so the reentrancy fix is code-traced not unit-tested; AudiouterCore/AGENTS.md is 2496 words against a 300-word budget (pre-existing, wants a separate trim); end-to-end grant->resume->audio still owed as a live check.

## T-ZOMBIE — launch-time auto-heal for stale PTP-helper registration — 2026-07-27
- [T1] done — added `func unregister() async throws` to PTPHelperManaging (PTPHelperService.swift, matching SMAppService.unregister()'s async-throws-only shape, no sync variant exists) with a doc comment mirroring register()'s idempotency + Developer-ID-gating note; implemented in SMAppServicePTPHelper as `try await service.unregister()`. Updated all 6 conformers found via `grep -rn ": PTPHelperManaging"` (matches planner's list exactly, none missed): SimulatedPTPHelper (PermissionMode.swift, no-op stub), SnapshotPTPHelper (onboarding-snapshot/main.swift, no-op stub), FakeHelperStatus (PTPHelperActivationTests.swift, no-op stub), FakePTPHelper in SetupModelTests.swift and OnboardingUITests.swift (both gained `unregisterCount` mirroring the existing `registerCount` pattern). No caller added (T2's job). swift build: exit 0, "Build complete!"; swift build --build-tests: "Build complete!" — both clean, only pre-existing unrelated Sendable-closure warnings in AppDelegate.swift/NativeCaptureCoordinatorTests.swift/PerAppCaptureCoordinatorTests.swift, nothing referencing unregister.
- [T6] done — scripts/purge-stale-ptp-helpers.sh created: dry-run-by-default lister + --apply bootout for every launchd label ending in `.ptphelper`. shellcheck clean (installed via brew, was absent). Live dry run on this dev machine found 16 real stale registrations across bundle-id variants (com.audiouter.Audiouter.*, .coexist, .RoutingConsolidationTest, several *test/*fix/*check ids) — matches the 16-purged figure in the T-ZOMBIE plan notes exactly. --apply NOT run (would mutate real daemons on this machine); apply-path match verified by static inspection to be scoped strictly to `*.ptphelper` (two independent case-guards, plus the enumeration awk filter uses exact string-suffix equality, not substring grep).
- [T2] reconciler — done — swift build (in AudiouterCore/): exit 0, "Build complete!", no new errors/warnings from this change (only pre-existing PopoverController main-actor + PTPHelperManaging non-Sendable Swift-5-mode notes, the latter mirroring the existing PTPHelperActivator pattern)
- [T4] launch heal wiring — done — swift build clean (Build complete!, no new warnings/errors; only pre-existing Sendable-closure warnings). Added healPTPHelperZombieIfNeeded() (@MainActor, native-backend-gated) in AppDelegate.swift, called right after registerPTPHelperIfNeeded() at launch; runs PTPHelperReconciler(helper: permissionProviders.ptpHelper).reconcile() in a detached Task so it never blocks launch. On .healed(.requiresApproval) or .healFailed, presents presentSetup(reason: .permissionLost([.ptpHelper])) guarded on onboardingWindowController == nil (matches the existing .permissionLost call-site guard). .notEnabled/.healthy/.indeterminate/.healed(.enabled) are logged no-ops. Doc comment explains why auditRequiredPermissionsIfNeeded (status-only) cannot catch this zombie state. No changes to PTPHelperService.swift or test files.
- [T5] done — rewrote T-ZOMBIE section in PLAN-AIRPLAY-COEXISTENCE.md from "ROADMAP follow-up; ships without it" to "DETECTION + AUTO-REPAIR BUILT"; added PTPHelperReconciler mechanism summary (XPC_ERROR_CONNECTION_INVALID zombie signal, launch-time one-shot, contention-safe .indeterminate, auto-repair + ptp_zombie_heal telemetry, .permissionLost fallback), status "built, unit-tested, live test owed (Developer ID signing required)"; reframed design rationale as "why this avoids false positives" not "why we defer"; added live-test checklist item for zombie repro using scripts/purge-stale-ptp-helpers.sh (bootout → relaunch, verify telemetry + recovery + setup-screen). AGENTS.md verified unchanged (PTPHelperService already listed).
- [T3] reconciler tests — done — new AudiouterCore/Tests/AudiouterCoreTests/PTPHelperReconcilerTests.swift (nested in SerializedSharedState, since it touches Telemetry._installTestSink), 15 @Test declarations / 11 test-run entries incl. parametrized cases: shouldHeal pure truth table (true only for .enabled+.zombie; false for every other status x probe combo), reconcile() orchestration via a RecordingHelper fake (logs unregister/register call order, status mutates after register()) + RecordingProbe fake (records invocation count) — non-.enabled statuses (.requiresApproval/.notRegistered/.notFound) return .notEnabled with the probe NEVER called and no unregister/register; (.enabled,.zombie) heals exactly once, unregister strictly before register, re-read status returned via .healed for both a post-register .enabled and .requiresApproval; (.enabled,.healthy)->.healthy and (.enabled,.indeterminate)->.indeterminate both heal zero times. Bonus: asserts the ptp_zombie_heal telemetry line fires only on the heal path, never on healthy/indeterminate/non-enabled. `swift test --filter PTPHelperReconcilerTests`: 11/11 passed in 0.035s. Full gate `./scripts/run-tests.sh`: exit 0, 1540 tests in 92 suites, all green. Zero real SMAppService/XPC touched — every double is an in-memory fake.

## Audio-routing consolidation (roadmap 007) — 2026-07-26
Baseline before any edits: main@fd820d5, 1510 tests / 89 suites, all green (67s).
Worktree: .claude/worktrees/audio-routing-consolidation-92be71, branch claude/audio-routing-consolidation-92be71.
- [T10] done — brought the 2026-07-26 re-evaluated architecture review onto this branch (docs/notes/architecture-review-audio-routing-2026-07-26.md, content from claude/audio-routing-architecture-review-3f1eff@6e1c0d1) and appended a new "Correction" section documenting what the implementation waves actually did versus its predictions: defect A landed as a deliberate PARTIAL consolidation (TapRebuildLifecycle.swift extracted only the provably-identical coalescer/reanchor pieces, not the claim/teardown/commit choreography); defect B closed for the scope-transition failure mode via AirPlayEngine's OutputBindResult/boundStreamId/rebindOutput, with two residual risks (scope exclusivity still GroupController-only, cross-FIFO ordering across an in-flight transition still unarbitrated) left explicitly open; defect C fixed by gating the functional TCC probe's fast path on code identity rather than removing the short-circuit outright; defect D consolidated behind the watcher-only DefaultOutputDeviceMonitor, matching the JUCE baseline the review cited. Also flagged a new open finding (per-app taps lack a device-identity-compare equivalent to the whole-system tap's dropout-prevention check) and recorded the unrelated T-DIAG telemetry-misattribution fix found along the way. Updated AudiouterCore/Sources/AudiouterCore/AGENTS.md (added DefaultOutputDeviceMonitor + TapRebuildLifecycle to the architecture diagram and type map) and AudiouterCore/AGENTS.md (corrected the TapRebuildDecision entry, which described per-tap raw HAL listener blocks that T2 replaced with DefaultOutputDeviceMonitor's shared fan-out). No AppRelauncher references found in either AGENTS.md file (T9 had already removed the only call site). Docs-only change; ROADMAP.jsonl and docs/plans/PLAN-AUDIO-ROUTING-CONSOLIDATION-LIVE-TESTS.md untouched per task scope.
- [D2-JUDDER-016] plan landed — 2026-07-29: whole-system audio dropout/judder investigation plan (docs/plans/PLAN-WHOLE-SYSTEM-AUDIO-DROPOUT-JUDDER-INVESTIGATION.md) landed on claude/judder-doc; catastrophic-dropout fixes already shipped to main @ e960a7b, owner checklist 7/7 live-verified; doc coordinates roadmap 016-019; instrumentation remainder (I1, branch claude/judder-instrumentation-remainder) in flight. D2 adversarial verification gate run against this doc (see commit history for anchor corrections).

## Routing arbiter (roadmap 008) — 2026-08-05
- [research] done — code-grounded map of the converging/bindTail domains landed as docs/notes/routing-arbiter-008-research.md: both scope-transition FIFOs traced (converge slot vs global bindTail chain, plus the enqueueRebindRecovery cross-link that already rides both), UI-only exclusivity walked (clearRoutes + popover menu filter; NativeBackend has no arbiter), six concrete in-flight interleavings enumerated (I1 TOCTOU double-read resurrects defect B concurrently; I4 unbind-kills-live-stream-0 while `added` still claims it; I5 recovery-backoff ping-pong), hermetic test seams inventoried (SpyEngine liveStreams/opDelayNanos/onAddOutputBody), branch contention checked (aggregate-device-wave3 = 5 unmerged NB commits, highest risk). Docs-only commit; no Swift touched.

2026-08-05 (design agent): Design committed (18a0c6b) — docs/plans/PLAN-ROUTING-ARBITER-008.md.
Chose a whole-system-priority scope arbiter as policy over EXISTING stateQueue state (no new
lock/map): R5 effective-route demotion extended to Selected Devices, stillOwnsRebind-style
fire-time checks on every bindTail op, per-app replay re-driven at WS slot release. Rejected:
merged serialization domain (PTP head-of-line blocking + 013-shape reentrancy) and blocking
converging-slot claims (provable FIFO deadlock). T1-T8 tasks + 8 deterministic race tests specced.

2026-08-05 (design reviewer): Adversarial review done — verdict REPAIRED, plan edited in place.
Found the draft's I1/I2 closure argument false (converge starting after a bind op does not order
its boundStreamId read after that op's addOutput — a written-out interleaving left the engine
stuck on stream 1 with the blanket unbind-downgrade freezing it forever), the recovery chains
ungated (performRebindRecovery never passes performBindOp's gate), a zombie-session regression
in the blanket downgrade (desiredOn-only park), a missing !suspended guard on re-drive, one
unforceable test (onAddOutputBody never fires in the per-app addOutput), one flaky-by-race test,
and a self-contradictory "both T7 tests keep passing" claim. Repairs: four-case unbind arm with
a verify-first settle reusing enqueueRebindRecovery's slot/gen discipline, pendingScopeSettles
consumed at release, stillOwnsRebind .perApp WS-claim conjunct + pre-op re-check, gate placed
after the PTP wait, test plan fixed + new test 9 forcing the surviving interleaving.

2026-08-05 (implementation agent): T1-T8 implemented. All three mechanisms landed in
NativeBackend.swift as designed — demote-at-decision (isRouteTargetEligibleLocked + edge-triggered
ScopeConflict record/telemetry inside effectiveAppRoutesLocked), selection-edge replay in
setOutputSet, the fire-time gate after ensurePTPTakeover + the FOUR-CASE unbind arm with the
verify-first settle on the shared recovery chain (verifyFirst flavor + pre-op preflight +
stillOwnsRebind .perApp WS-claim conjunct), and release-side re-drive (ConvergeReleaseAction:
per-app replay via generalized replayPendingPerAppBindings(trigger:) + pendingScopeSettles
consumed by RE-ENQUEUING the deferred unbind through the four-case arm — deviation §6.1, closes
the parked-converge zombie leak the inline case-4-only settle would have missed). 9 race tests
added (each interleaving forced via hold-open SpyEngine hooks / an armable PTP activator /
telemetry-observed classification); deviceMovingFromWholeSystemToPerApp rewritten to the
sanctioned deselect-first direction; deviceFedBySystemAndStreamReportsMax rebuilt (its fixture
was the now-prevented double-claim state). Four deviations documented in the plan's new §6;
AGENTS.md arbiter rule + two traps added. NativeBackendTests 192/192 green; full suite running.

2026-08-05 (final adversarial reviewer): independent verification pass done — every commit's diff
audited against the reviewed design (no undocumented deviations beyond the plan's own §6 four),
concurrency re-attacked with the real code, losing-domain telemetry force-read, single-domain
op traces re-confirmed pinned, tests 3+9 confirmed honest by running them red against a locally
neutered four-case arm. ONE real defect found + fixed: the release-side settle consumption
re-enqueued a deferred .unbind unconditionally — after a deselect-while-converging the restored
route's .bind sits FIFO-ahead, so the stale unbind killed the freshly re-engaged per-app session
with streamBindings still set (silent stranding, no replay guard could ever re-drive it). Fix:
consume drops the settle loudly (unbind_redrive outcome=dropped_route_reengaged) when
streamBindings[id] != nil; atomic vs the restore replay under stateQueue in both orders. New
test 10 staleDeferredUnbindMustNotKillAReengagedRoute forces the interleaving (held rebind →
deferred unbind → deselect → bind parked pre-gate on the armed PTP activator → release):
confirmed red pre-fix (session dead, remove after the re-engaged bind), green post-fix.
Plan §6b documents the review. Full suite 1645/1645 green via scripts/run-tests.sh.

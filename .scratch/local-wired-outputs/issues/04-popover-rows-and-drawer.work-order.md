Revised after team scoping: sink, ui, routing

# Work order — Ticket 04: wired rows under "This Mac", glyphs, sync drawer and wizard

Worktree: `/Users/alechenderson/Projects/AirPlay Controller/.claude/worktrees/cpi-consumption-analysis-2e6406`. Paths below are relative to it unless absolute. Ticket 02 is built on disk (uncommitted); amendment 02b (scope-sink) will make a used wired row stay greyed instead of removed. This track forks from the commit that lands 02 + 02b.

## Goal

Make a `.wired` row (ticket 02) render as one of the Mac's own outputs: listed directly under the pinned "This Mac" row inside the card body, carrying the same sync chip, drawer and alignment wizard a Bluetooth row carries, kept selected and greyed through an unplug until replug, and selectable alone with "This Mac" auto-deselected. Also let the companion snapshot carry an alignment block for a wired row. This is the popover/row-view/snapshot layer only. The popover already routes every non-local, non-Cast trimmable row through the Bluetooth closures keyed by device id, and scope-sink confirmed those closures have no kind guard on the backend (`NativeBackend+Bluetooth.swift:1285-1342, 2450-2560, 1348-1450`), so the change is a handful of kind gates, not a second implementation.

## Verified facts

- Ticket 02 on disk: `Device.Kind.wired` (`AudioutCore/Sources/AudioutCore/Device.swift:39`), `Device.WiredTransport: String, CaseIterable` with `builtInSpeakers, headphoneJack, usb, hdmi, other` (`:101`), `Device.isWired` (`:156`), `Device.wiredTransport` (`:213`, last `init` parameter). Per-transport glyphs are in `Device.symbolName`. Mock fixtures `usb-dac` and `ext-headphones` are appended in `OutputBackend.swift`'s mock arm. Amendment 02b (scope-sink) changes `NativeBackend+Wired.swift:31-40` so a used row (selected, app-routed, or saved-group member) is committed `isAvailable = false`, `connectionState = .off` and kept; an unused row is removed. Treat 02b as existing.
- Row glyph: `DeviceIconController.symbolName(for:)` returns `device.symbolName` unless the user set an override — `AudioutCore/Sources/AudioutSharedUI/DeviceIcon.swift:164-166`; fed at `PopoverController.swift:2972,3031`. Glyphs are done by ticket 02; nothing here touches icons.
- Section grouping: `deviceSections()` at `AudioutCore/Sources/AudioutPopoverUI/PopoverController.swift:2137-2151`. The "This Mac" section is `visible.filter(\.isLocalDevice)` (`:2143`); the AirPlay section is `!isLocalDevice && !isBluetooth && !isCast` (`:2145`), so today a wired row lands under "AirPlay Speakers". `orderedDevices()` sorts by `(name, id)` (`:1914-1916`). `rebuild()` renders every device in the "This Mac" section as a pinned row in the card body with `indented: false` (`:1794-1798`); that section never renders a header (`:1945-1946`). `renderedDeviceOrder()` flattens the sections (`:2158-2160`). A kept unavailable wired row still passes the `visible` filter (`:2138-2140`, only Bluetooth is listing-gated) and `DeviceRowView` greys it with no wired code: checkbox enabled only while selected (`DeviceRowView.swift:567`), fader/mute disabled (`:733-734`), chip disabled (`:783`), "Unavailable" feed glyph (`:1206-1212`).
- The "−" footer, the "−" menu and the first-run hint read only non-Mac sections (`:1825-1827`, `:3199`, `:2922`). A wired row in the Mac section is therefore not hideable and does not count as "a speaker besides the Mac". Accepted.
- Deselect-on-loss edge at `PopoverController.swift:923-928` is `device.isBluetooth && !device.isAvailable`. It STAYS Bluetooth-only (team call): a selected wired row keeps its selection through unplug, which is what makes it "used" for 02b's keep rule and what ticket 03's replug re-arm (`expectedSelected.contains(id)`) needs. The route reset at `:964` guards `devicesByID[goneID] == nil`, so a kept row never trips it (scope-routing).
- Auto-swap and lone selection already hold for any non-local kind: `GroupController.setDeviceSelected` drops the local device when `!d.isLocalDevice` and `selectedDeviceIDs == [local]` (`AudioutCore/Sources/AudioutCore/GroupController.swift:408-416`); `isPassthrough` is `selectedDeviceIDs == [local]` (`:525-528`). Proven by `GroupControllerTests.swift:353`. Decision 7 needs no code.
- Sync controls: `isTrimmable(_:)` is `device.isBluetooth || device.isLocalDevice || device.isCast` (`PopoverController.swift:3083`), passed as `showsSyncControls:` at `:2822`. `btSyncTrim(for:)` (`:3057-3075`), `applyBTTrim` (`PopoverController+SyncDrawer.swift:166-193`), `btUsableTrimRange` (`:139-160`), `syncDrawerDidRequestResetAlignment` (`:305-326`) branch on `isLocalDevice`/`isCast` and send everything else to the `bt*` closures keyed by id. `canResetAlignment` (`:124-131`) admits any non-local non-Cast row. Measured-latency read is gated `device.isBluetooth ? btMeasuredLatency(for:) : nil` at `PopoverController.swift:2974` and `:3033`. Post-reset auto-collapse is gated `isBluetooth == true, isCast == false` at `PopoverController+SyncDrawer.swift:321`.
- Wizard: `startBTAlignmentWizard` (`PopoverController+BTWizard.swift:167-341`) refuses only Cast (`:172`) and branches on `isLocalTarget` (`:189`); reference and target carry `isBluetooth:` (`:234-237`, `:505`), compared by `BTAlignmentWizardSession.pairSoundsDiffer` (`BTAlignmentWizardSession.swift:107-110`); `introCopyNamingSounds` gives the target the bright sound when `targetIsBluetooth` (`BTAlignmentWizardView.swift:60-66`). A wired sink lives inside the Bluetooth manager after ticket 03 and hears the bright click (`AlignmentTickInjector.swift:635-637`, `NativeCaptureCoordinator.swift:1987-2004`, scope-sink), so `isBluetooth || isWired` is the right flag. `bt_sync:wizard_started` sends `target: isLocalTarget ? "local" : "bluetooth"` (`:337-339`). Reference candidates `btWizardReferenceDevices` (`:439-446`) already include a wired row through the middle arm, but its doc (`:429-431`) promises row-render order, and after this ticket wired rows render before AirPlay. The first-join note gate (`:149`) stays Bluetooth-only (the backend does not offer the note for wired).
- Row view: `DeviceRowView.chipOffersWizard` is `device.isBluetooth && !device.isCast && !device.isLocalDevice && !(syncTrimIsSet || syncMeasuredLatencyMs != nil)` — `AudioutCore/Sources/AudioutSharedUI/DeviceRowView.swift:1967-1970`. Chip tooltip else-branch (`:2084-2090`) covers any non-local non-Cast row. `supportsAlignmentWizard` is `showsSyncControls && !device.isCast` (`:2208`). The greyed-row reconnect branches (`:830`, `:2326`, `:2411`) stay Bluetooth-only: they kick a baseband reconnect that means nothing for a jack; a greyed wired row's name click falls to `guard enableCheckbox.isEnabled` (`:2331`), so it can only deselect.
- Companion snapshot: `CompanionSnapshotBuilder.alignmentState` (`AudioutCore/Sources/AudioutCore/CompanionSnapshotBuilder.swift:232-`) guards `device.kind == .bluetooth` at `:234`; its doc at `:233-235` says "`nil` for every non-Bluetooth device". The report source has no kind guard (`btAlignmentReport(forDevice:)` → `btSpeakerTiming.report(uid:)`, `NativeBackend+Bluetooth.swift:1474-1476`; a uid with no link record reads `.steady`, `BTSpeakerTiming.swift:591-595`). `CompanionSnapshotBuilderTests.swift:649-650` asserts the local row's `alignment == nil` with message "only Bluetooth rows ever carry an alignment".
- Analytics: `deviceRow(_:didToggleEnabled:for:)` at `PopoverController.swift:3599-3611` builds `props["kind"]` and fires `mixer:device_selected` / `mixer:device_deselected`. `bt_sync:trim_committed` fires with no properties at `PopoverController+SyncDrawer.swift:186`. Event table: `/Users/alechenderson/Projects/audiout-shared/docs/analytics-events.md` — `mixer:device_selected` row line 57, `bt_sync:trim_committed` line 88, `bt_sync:wizard_started` line 91 (`target`: `local` or `bluetooth`).
- Folder rules to amend with the code: `AudioutCore/Sources/AudioutPopoverUI/AGENTS.md` line "A never-aligned Bluetooth row's chip IS the wizard's door; a measured one opens the drawer."; `AudioutCore/Sources/AudioutSharedUI/AGENTS.md` line "A never-measured Bluetooth row's SYNC chip is the alignment wizard's door, not a readout." The popover AGENTS.md "loses availability" line stays as is.
- Test seams: `PopoverDeviceVisibilityTests.swift` (`@Suite(.serialized) struct`, `makePopover(fleet:)` `:24-40`, `local()`/`airplay()`/`bt()`/`cast()` helpers `:42-58`, `test_renderedDeviceIDs()`, `test_subsectionTitles()`, `test_deviceRow(for:)`); `BTRowsUITests.swift` (`SpyDelegate` `:25-48`, `btDevice()` `:51-55`, `castDevice()` `:61-63`, `makeRow` `:66-80`, chip-door test `:390-403`, deselect-on-loss test `:890-911`, drawer-commit test `:789-812`). Hooks: `test_toggleDeviceEnabled(deviceID:on:)` (`PopoverController+TestSupport.swift:410`), `test_toggleSyncDrawer(deviceID:)` (`:458`), `test_syncDrawer` (`:470`), `DeviceRowView.test_showsSyncControls` (`DeviceRowView+TestSupport.swift:471`), `DeviceRowView.test_isEnabledOn` (`:179`, `enableCheckbox.state == .on`), `test_fireSyncChipClick`, `BTSyncDrawerView.test_firePlusClick`/`test_shiftModifierOverride` (used at `BTRowsUITests.swift:805-806`).

## Steps

Decisions, final: wired rows sort by name after the Mac row inside the "This Mac" section; no new `wired:*` events (the ticket's proposed `wired:row_selected` is the existing `mixer:device_selected` with `kind: wired`, which gains a `transport` property instead); `bt_sync:wizard_started`'s `target` gains the value `wired`; `bt_sync:trim_committed` gains a `target` property (`local`, `bluetooth`, `wired`); wizard target/reference use `isBluetooth || isWired`; the first-join note stays Bluetooth-only; the deselect-on-loss edge stays Bluetooth-only; wired rows are not hideable.

1. **Register the analytics changes first** in `/Users/alechenderson/Projects/audiout-shared/docs/analytics-events.md` (edit, do not commit): line 57 — add `transport` to the properties column with values `builtInSpeakers`, `headphoneJack`, `usb`, `hdmi`, `other`, "only when `kind` is `wired`"; line 88 — add `target` with values `local`, `bluetooth`, `wired` for the `bt_sync:trim_committed` name (the Cast name keeps none); line 91 — add `wired` to `target`'s values.

2. **Write the tests, run them, paste the failing output.**
   - `AudioutCore/Tests/AudioutCoreTests/PopoverDeviceVisibilityTests.swift`: add a `wired(_ id:name:transport:available:)` helper beside `cast` (`:56-58`) building `Device(id:name:kind: .wired, isAvailable:supportsAirPlay2: false, wiredTransport:)`, and one test `wiredRowsSitUnderTheMacAndFollowTheSnapshot` — defect: a wired row rendered in the AirPlay subsection, where it draws as an AirPlay speaker with no sync chip. `popover.update(devices:)` with `[local(), airplay(), bt("bt-a:output", name: "Speaker A"), wired("ext-headphones", name: "External Headphones", transport: .headphoneJack)]`; expect `test_renderedDeviceIDs() == ["mac", "ext-headphones", "office", "bt-a:output"]`, `test_subsectionTitles() == [airPlayTitle, bluetoothTitle]`, `test_deviceRow(for: "ext-headphones")?.test_showsSyncControls == true`. Then update with the headphones replaced by `wired("builtin-speakers", name: "MacBook Pro Speakers", transport: .builtInSpeakers)` (the default moved and the unused row left); expect `["mac", "builtin-speakers", "office", "bt-a:output"]` and `test_deviceRow(for: "ext-headphones") == nil`.
   - `AudioutCore/Tests/AudioutCoreTests/BTRowsUITests.swift`: add a `wiredDevice(available:)` helper beside `castDevice()` returning `Device(id: "BuiltInHeadphoneOutputDevice", name: "External Headphones", kind: .wired, isAvailable: available, supportsAirPlay2: false, wiredTransport: .headphoneJack)`, and three tests:
     - `anUntunedWiredChipIsTheWizardDoor` — defect: a never-aligned wired row's chip opens an empty drawer instead of the wizard. Mirror `:390-403` with `wiredDevice()`: chip title `"Align by ear"`, `test_fireSyncChipClick()`, expect `spy.wizardRequests.map(\.id) == [wiredDevice().id]`, door `.chip`, `drawerToggles.isEmpty`.
     - `selectedWiredRowStaysSelectedThroughUnplug` — defect: a deselect on unplug that makes replug come back silent (the backend keeps and re-arms only a still-selected row). `makePopover(fleet: [local(), wiredDevice()])`, `update(devices:)` same, `_ = popover.test_toggleDeviceEnabled(deviceID: wiredDevice().id, on: true)`, expect selected; update with `[local(), wiredDevice(available: false)]`; expect `controller.selectedDeviceIDs.contains(wiredDevice().id)` and `popover.test_deviceRow(for: wiredDevice().id)?.test_isEnabledOn == true`. This test passes before the change (the edge is already Bluetooth-only); it pins the decision, so paste its passing output and say so.
     - `wiredTrimEditsFlowThroughTheBluetoothClosureKeyedByUID` — defect: a wired row's drawer edit reaching the local or Cast closure, or the wrong id. Mirror `:789-808` with `wiredDevice()`: `btTrimProvider = { _ in 120 }`, `btTrimIsSetProvider = { _ in true }`, record `onSetBTTrim`; expect chip `"120 ms"`; `test_toggleSyncDrawer`, `test_shiftModifierOverride = true`, `test_firePlusClick()`; expect `written.last` is `(130, wiredDevice().id, true)`.
   - `AudioutCore/Tests/AudioutCoreTests/CompanionSnapshotBuilderTests.swift:650`: change the message to "only Bluetooth and wired rows ever carry an alignment". (No new test: ticket 05's snapshot test covers the wired row; its `alignmentFor` returns nil, so `alignment == nil` still holds there.)
   - Run `bash scripts/run-tests.sh --filter "PopoverDeviceVisibilityTests|BTRowsUITests"` (timeout 600000) and paste the output: the placement, chip-door and drawer tests must fail; the stays-selected test passes.

3. **`AudioutCore/Sources/AudioutPopoverUI/PopoverController.swift`**:
   - `deviceSections()` `:2143`: the "This Mac" section becomes the visible local device(s) followed by `visible.filter(\.isWired)` (already name-sorted). `:2145`: add `&& !$0.isWired` to the AirPlay filter. Extend the doc comment at `:2122-2125` with one sentence: wired outputs list under the Mac's pinned row (decision 5).
   - `update(devices:)`: leave `:924` as is. Extend the comment at `:911-922` with one sentence: a wired row keeps its selection through unplug because the backend keeps the greyed row and re-arms it on replug.
   - `:2974` and `:3033`: `syncMeasuredLatencyMs:` gate becomes `(device.isBluetooth || device.isWired) ? btMeasuredLatency(for: device.id) : nil`.
   - `isTrimmable` `:3083`: add `|| device.isWired`; doc (`:3078-3081`) names wired outputs.
   - `deviceRow(_:didToggleEnabled:for:)` `:3608-3611`: when `devicesByID[id]?.wiredTransport` is non-nil, set `props["transport"]` to its `rawValue`.

4. **`AudioutCore/Sources/AudioutPopoverUI/PopoverController+SyncDrawer.swift`**:
   - `:186`: for the non-Cast name, pass `["target": …]` with `"local"` when `isLocalDevice`, `"wired"` when `isWired`, else `"bluetooth"`.
   - `:321`: the auto-collapse gate becomes: Bluetooth or wired, and not Cast. Extend the comment at `:318-320`.

5. **`AudioutCore/Sources/AudioutPopoverUI/PopoverController+BTWizard.swift`**:
   - `:235` and `:505`: `isBluetooth: $0.isBluetooth || $0.isWired` (reference); `:237`: `targetIsBluetooth: device.isBluetooth || device.isWired`. One comment line at `:230-233`: a wired sink is fed by the same fan-out as Bluetooth, so it plays the Bluetooth-side sound.
   - `:338`: `target` becomes `isLocalTarget ? "local" : (device.isWired ? "wired" : "bluetooth")`.
   - `:443-445` `btWizardReferenceDevices`: the list becomes locals, then `ordered.filter(\.isWired)`, then `ordered.filter { !$0.isLocalDevice && !$0.isBluetooth && !$0.isCast && !$0.isWired }`, then `orderedBluetoothDevices(in: ordered)`; the doc at `:429-431` lists the order as "locals, wired, AirPlay, Bluetooth".

6. **`AudioutCore/Sources/AudioutSharedUI/DeviceRowView.swift`** `chipOffersWizard` `:1967-1970`: `(device.isBluetooth || device.isWired) && !device.isCast && !device.isLocalDevice && …`; doc (`:1963-1966`) says "Bluetooth and wired speakers only".

7. **`AudioutCore/Sources/AudioutCore/CompanionSnapshotBuilder.swift`** `:234`: the guard becomes `guard device.kind == .bluetooth || device.kind == .wired, let report = alignmentFor(device) else { return nil }`; doc at `:233-235` reads "`nil` for every device that is neither Bluetooth nor wired, and for one the caller has no report for".

8. **AGENTS.md lines** (docs land with code): `AudioutCore/Sources/AudioutPopoverUI/AGENTS.md` — "A never-aligned Bluetooth or wired row's chip IS the wizard's door; a measured one opens the drawer."; `AudioutCore/Sources/AudioutSharedUI/AGENTS.md` — "A never-measured Bluetooth or wired row's SYNC chip is the alignment wizard's door, not a readout."

9. **`.scratch/local-wired-outputs/issues/04-popover-rows-and-drawer.md`**: line 3 → `Status: ready-for-human (built 2026-09-26; live check owed)`; line 9-10 "Selected-then-unplugged → deselected on the edge" → "Selected-then-unplugged stays selected and greyed until replug (team call 2026-09-26)"; under Analytics (`:20-23`) replace the `wired:row_selected` sentence with the decision above.

10. Run the Verification commands.

## Out of scope — do not touch

- Ticket 03 and 02b: `NativeBackend+Wired.swift`, `NativeBackend+Bluetooth.swift` (including the drift closure at `:1067-1070`, the arm gate at `:1399`, `referenceIsBluetooth` at `:1539`), `BTDeviceSink`, `BTSyncedSink`, `BTTrimStore`, `LocalOutputLatency`, the `BTOutputControlling` methods, and the `popoverController.bt* = ` / `onBT* =` wiring in `AudioutApp/AppDelegate.swift:1181-1274`. The popover's closures are the seam; add no wired-specific closures.
- Ticket 05: `isRouteTargetReachableLocked`, `AppRouteTargetEligibility`, `GroupStore`, `NativeBackend+PerAppRouting.swift`, `AppDelegate.swift:2753`, `CompanionSnapshotBuilder.alignmentReferenceID` (`:220-231`) and every other line of that file beyond step 7, and the route-reset block at `PopoverController.swift:930-970`.
- Ticket 06 and the public aggregate's sub-device choice.
- `PopoverController.swift:924` (stays Bluetooth-only); `GroupController.setDeviceSelected` / `isPassthrough`; `Device.symbolName` / `DeviceIcon.swift`; the first-join note gate at `PopoverController+BTWizard.swift:149`; the reconnect branches at `DeviceRowView.swift:830,2326,2411`; `BTAlignmentWizardSession`, `BTAlignmentWizardView` copy; the "−" footer/menu and hidden-speakers logic; the popover AGENTS.md "loses availability" line.
- No new `Analytics` event names, no `wired:*` events, no properties carrying device names or UIDs.
- No cleanup, no abstractions, no error handling for impossible cases, no backwards-compat shims. Do not commit or push in either repository.

## Verification

Test seam: `PopoverDeviceVisibilityTests` (placement and the swap), `BTRowsUITests` (chip door, stays-selected, drawer edit keyed by UID), `CompanionSnapshotBuilderTests` (widened alignment guard); defects stated per test in step 2.

```
bash scripts/run-tests.sh --filter "PopoverDeviceVisibilityTests|BTRowsUITests|PopoverBTAlignmentUITests|PopoverCastSyncOffsetTests|PopoverLocalSyncTrimTests|CompanionSnapshotBuilderTests"
```
→ all pass; the run must print `Test run with N tests` with N > 0 (a no-match filter reports green). `PopoverBTAlignmentUITests`, `PopoverCastSyncOffsetTests`, `PopoverLocalSyncTrimTests` are included because steps 3-5 edit gates their Cast and local paths pass through.

Foreground, timeout 600000. On timeout: `bash scripts/capacity.sh status` once, retry once, then stop and report `blocked on capacity` with that output.

## Execution plan

One track, all steps. Files: `PopoverController.swift`, `PopoverController+SyncDrawer.swift`, `PopoverController+BTWizard.swift`, `DeviceRowView.swift`, `CompanionSnapshotBuilder.swift` (line 234 only), two AGENTS.md files, three test files, the ticket file, and `analytics-events.md` in the audiout-shared checkout. Model: sonnet. Effort: medium — every edit is a one-line gate at a cited line; the tests copy cited neighbours. SERIAL after ticket 02 + 02b land (consumes `Device.isWired`, `Device.wiredTransport`, the keep-while-used row). PARALLEL with tickets 03 and 05: 03 owns the sink and `NativeBackend+Bluetooth.swift`; 05 owns `NativeBackend+PerAppRouting.swift`, `AppDelegate.swift:2753` and the rest of `CompanionSnapshotBuilder.swift` — the runner merges the one-line overlap at `:234` if 05 also edits that file. The tree currently holds ticket 02's uncommitted edits.

## Executor rules (copy verbatim into the handoff prompt)
> - Follow the steps in order. Do not add, merge, reorder, or skip steps.
> - Before editing in any folder, read the nearest AGENTS.md above it (and the root one) if the repo has them — folder rules and traps bind even when the work order doesn't repeat them.
> - If reality contradicts a Verified fact or a step is impossible as written, STOP and report the discrepancy. Do not improvise a workaround.
> - Before reporting progress, audit each claim against a tool result from this session. Only report work you can point to evidence for. If tests fail, say so with the output.
> - If the work order names a new test, run it before making the change and paste the failing output. A test that passes before the change proves nothing.
> - "Done" means the Verification commands were run in this session and passed. Paste their output.
> - Touch nothing in the Out-of-scope list.
> - Deliver what was asked, at the scope intended. If the spec seems mistaken or a better approach exists, say so in a sentence and continue as specified rather than quietly narrowing, widening, or transforming it.

## Cross-area questions

None. (Questions 1-3 of the first draft are answered by scope-sink: trim, latency, wizard-preview and mic-probe closures are id-keyed with no kind guard; a wired sink hears the bright tick.)
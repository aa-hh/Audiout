Confirmed against the code: `lastGroupTargets` (NativeBackend.swift:1391) holds every saved group's members, so the keep test can live in the backend. My earlier "backend is blind to saved groups" was wrong; folded in below.

## Answers

**Q1. The three filters 03 names.**

- `PopoverController+BTWizard.swift:444` (reference candidates). A wired row already lands in the middle arm (`!$0.isLocalDevice && !$0.isBluetooth && !$0.isCast`, :444), so it is offered as a reference today; the local device still wins the default (`btWizardDefaultReference`, :453-454). No widening for inclusion. One ordering fix belongs in 04: the doc at :430-431 promises "the same order the rows themselves render (locals, AirPlay, Bluetooth)", and after step 3 wired rows render under the Mac, ahead of AirPlay. Add to step 5: at :443-445 make the list `ordered.filter(\.isLocalDevice) + ordered.filter(\.isWired) + ordered.filter { !$0.isLocalDevice && !$0.isBluetooth && !$0.isCast && !$0.isWired } + orderedBluetoothDevices(in: ordered)`, and add "wired" to the doc's order list.
- `DriftCorrectionApplier`'s `isBluetooth` closure (`NativeBackend+Bluetooth.swift:1067-1070`). Not 04's: 04's out-of-scope names the whole file, and 03's out-of-scope hands the closure to 04, so nobody owns it. It is a live defect once 03 step 4 puts wired uids into `btSelectedUIDs`: `refreshDriftTrackingLocked` (:1176-1178) builds baselines from `btDriftBaselines(uids: btSelectedUIDs, latencies:)` (:1130-1137), which tags any selected uid with a measured latency as `kind: .bluetooth`; a wizard-measured wired row is then sampled and every correction hits `guard isBluetooth(uid)` (`DriftCorrectionApplier.swift:176-178`) and logs `drift_correction_refused reason: not_bluetooth`. Fix belongs in 03: widen the closure to `isBluetooth == true || isWired == true` (or filter wired out of the baselines). Sent to scope-sink.
- `startCompanionAlignmentProbe`'s `referenceIsBluetooth` (:1539). Not 04's (same file). Should widen to `|| isWired == true`: a wired reference renders inside the same manager, so it needs the sweep-free feed that `fanOutSplitToBT` gives every non-owner uid (`NativeCaptureCoordinator.swift:2564-2568`). No behaviour change in practice because `otherBTAudible` (:1538) reads `btSelectedUIDs`, which 03 widens, and a reference must be selected to be offered; the widening is for honesty. Sent to scope-sink.

**Q2. Wizard arm gate on `btSelectedUIDs` (:1399).** Acceptable when plugged: wired sinks are `BTDeviceSink`s, so `renderingDeviceUIDs()` reports them and `pollWizardArmGate` (:2640-2650) releases. It gets worse under the new decision with the deselect edge left Bluetooth-only: a selected, unplugged wired uid stays in `btSelectedUIDs` (03's `sinkUIDs` has no availability test, mirroring `NativeBackend+Tone.swift:507`), `setDevices` drops it, it never appears in `rendering`, and every wizard run waits `wizardArmCeilingSeconds` (`NativeBackend.swift:668`, 8 s) of bed. Today that only happens for a "play when up" Bluetooth select; after the decision it is the ordinary unplugged-headphones state. Proposed 03 edit at :1399: `Set(self.btSelectedUIDs.filter { self.known[$0]?.isAvailable == true })`. Sent to scope-sink.

**Q3. A kept greyed wired row in the UI.**

- Popover: `deviceSections()` lists any non-Bluetooth row that is present (`PopoverController.swift:2138-2140`), so a kept row renders under the Mac with step 3's section edit. `DeviceRowView` draws it greyed with no wired-specific code: checkbox enabled only while selected (`:567`), fader and mute disabled (`:733-734`), sync chip disabled (`:783`), "Unavailable" sublabel or feed glyph (`:1110`, `:1206-1212` returns `!isBluetooth` for `.connecting`, so wired reads Unavailable even mid-connect), hollow dimmed node (`:839`, `:809`).
- The reconnect branches at `:830`, `:2326`, `:2411` must stay Bluetooth-only. They fire `deviceRowDidRequestReconnect` → `groupController.requestReconnect` → `retryOutput` (`PopoverController.swift:3644-3647`), a baseband reconnect kick that means nothing for a jack; a greyed wired row's name click falls through to `guard enableCheckbox.isEnabled` (`:2331`), so unselected = no-op, selected = deselect. What a greyed wired row offers: deselect if selected, nothing else, until replug.
- Group editor: rows come from `devices` (`GroupEditorViewController.swift:826`), so a kept row renders through `MembershipRowView` dimmed with the "unavailable" label (`MembershipRowView.swift:275-276`, `:295`, `:322`), same as a dropped AirPlay device. No placeholder needed, provided the backend keeps the row. scope-routing confirms the backend keep test can read saved groups through `lastGroupTargets` (`NativeBackend.swift:1391`, filled from every saved group by `resolveGroupTargets`, `AppRoutingController.swift:246-253`).
- Deselect-on-loss loop: if 04 widened `:924` to wired, the edge would deselect the row on the same `deviceUpdated` that greys it, "selected" could never keep a wired row, and 03's replug re-arm (`expectedSelected.contains(id)`) would come back unselected. That is a bug against the decision, not intent. Rule: leave `:924` Bluetooth-only; a selected wired row stays selected through unplug and auto-resumes on replug, the same as a Bluetooth "play when up" select. Both teammates concur.

**Q4. `CompanionSnapshotBuilder.alignmentState` `kind == .bluetooth` (:234).** Widen in 04. The report source has no kind guard (`btAlignmentReport(forDevice:)` → `btSpeakerTiming.report(uid:)`, `NativeBackend+Bluetooth.swift:1474-1476`) and a uid with no link record reads `clockState == .steady` (`BTSpeakerTiming.swift:591-595`), so the phone would get an honest block; the phone's "Fix timing" runs `startCompanionAlignmentProbe`, whose `targetIsLive` is `btSelectedUIDs.contains(targetID)` (:1537), true for a wired row after 03. Step text: `CompanionSnapshotBuilder.swift:234` becomes `guard device.kind == .bluetooth || device.kind == .wired, let report = alignmentFor(device) else { return nil }`; doc at :224-225 says "for every device that is neither Bluetooth nor wired". `CompanionSnapshotBuilderTests.swift:650`'s message becomes "only Bluetooth and wired rows ever carry an alignment". 05's new test keeps `alignment == nil` (its `alignmentFor` returns nil) but its defect text "a row the Mac cannot measure" must go.

## Corrections to the draft

- **Step 2, `selectedWiredRowIsDeselectedOnAvailabilityLoss`**: delete. Contradicts the new decision (see Q3). Replace with `selectedWiredRowStaysSelectedThroughUnplug`: same setup, update with `wiredDevice(available: false)`, expect `controller.selectedDeviceIDs.contains(wiredDevice().id)` and `test_deviceRow(for:)?.test_isSelectedInSet == true` (hook name to confirm in `DeviceRowView+TestSupport.swift`); defect: a deselect on unplug that makes replug come back silent.
- **Step 3, second bullet (`:924`)**: delete. `:924` stays `device.isBluetooth`. Extend the comment at `:911` instead: a wired row keeps its selection through unplug because the backend keeps the row and re-arms it on replug.
- **Step 5**: add the `:443-445` ordering edit from Q1.
- **Step 7**: drop the "A selected Bluetooth or wired device that loses availability…" change; that AGENTS.md line stays as is. Keep the two chip-door lines.
- **Add a step** for `CompanionSnapshotBuilder.swift:234` and `CompanionSnapshotBuilderTests.swift:650` (Q4 text), and remove `CompanionSnapshotBuilder` from 04's out-of-scope list (05's out-of-scope already hands the guard to 04, so it is orphaned otherwise). Add `CompanionSnapshotBuilderTests` to the Verification filter.
- **Goal / Verified facts line 15**: "deselected on the availability edge when unplugged" is no longer true; rewrite to "kept selected and greyed until replug".
- **Cross-area question 3** is answered: wired hears the bright click (scope-sink, `AlignmentTickInjector.swift:635-637`, `NativeCaptureCoordinator.swift:1987-2004`), so step 5's `isBluetooth || isWired` stands. Questions 1 and 2 answered yes by scope-sink; steps 3-5 stand.
- **Verified facts line 14 ("rows leave on unplug")** and **line 11 (ticket 02 "then deviceRemoved")**: now conditional on the row being unused.

All other cited lines (`:2143-2145`, `:2974`, `:3033`, `:3083`, `:3599-3611`, SyncDrawer `:186`, `:321`, BTWizard `:235-237`, `:338`, `:505`, DeviceRowView `:1967-1970`, `:2208`, test seams `:42-58`, `:51-63`, `:390-403`, `:890-911`) verified on disk.

## Sent to teammates

- scope-sink: the drift applier closure at `NativeBackend+Bluetooth.swift:1067-1070` is owned by neither draft and refuses wired corrections once `btSelectedUIDs` carries wired uids; assign to 03.
- scope-sink: `referenceIsBluetooth` at `:1539` should widen (honesty; behaviour already covered via `otherBTAudible`).
- scope-sink: arm gate at `:1399` should filter `btSelectedUIDs` to available uids, else an unplugged-but-selected wired row costs every wizard run the 8 s ceiling; and a kept row needs `connectionState = .off`.
- scope-routing: agreed on leaving `:924` Bluetooth-only; raised the saved-group visibility question (answered: `lastGroupTargets`, no popover filter wanted).

## Still open

- Whether the drift closure is widened (wired rows get passive drift correction) or wired uids are filtered out of the baselines. Both are 03 edits; product call for the scoper.
- The exact `DeviceRowView+TestSupport` hook name for "selected in set" for the replacement test; I did not open that file.
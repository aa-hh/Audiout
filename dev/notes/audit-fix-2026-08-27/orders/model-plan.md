# Audit-fix execution plan — tracks and model assignments

## LIVE STATE (2026-08-27)
All 10 executors LAUNCHED, each in its own worktree at "/Users/alechenderson/Projects/AirPlay Controller/.claude/worktrees/fix-*" on branch claude/fix-* from main 59f2b044, pushed to origin. Final models: opus = T1,T2,T3a,T4,T5(upgraded per scoper),T6,T7,T8; sonnet = T3b,T9. Mule config was broken (audiouter.*→audiout.* key rename) — FIXED in shared .git/config; testPrefer=remote restored.
Next per track: Fable work-order-reviewer on completion → merge sequence proposed to Alec (NO merge without his go-ahead).
Proposed merge order (small/foundational first, conflict-heavy last): T2 → T1 → T9 → T8 → T3b → T3a → T7 → T5 → T4 → T6.
WRAP-UP PASS after merges: regenerate 3 snapshot sets (popover/settings/onboarding; window goldens NEVER); PRODUCT.md "Main Out"→"Main Audio"; wire T3a's localNetworkDeniedProvider seam in AppDelegate; audiout://register URL handler (make-app.sh plist + AppDelegate + sheet prefill); Group-N naming helper consolidation; percent-formatter consolidation (T3b/T8 dupes); PanelVC:773 stale comment; DeviceIcon cache adoption sites if T5 deferred any; roadmap.js status updates.
DECISIONS TAKEN AS DEFAULTS (Alec can veto at review): T7 copyright "© 2026 Alec Henderson" + HUD top-right pill; T6 Speaker Sync skippable + skip-ratchet; T5 delete-copy corrected wording + duplicate-name refusal; T9 lock-test supersession + rings stay + orphan-font ledger; T3a 0.6s reveal ceiling + quit glyph.

Pipeline per track: fable-scoper (Fable, fixed) → work-order-executor (model below) → work-order-reviewer (Fable, fixed).
Executors run in isolated per-track worktrees branched from main HEAD. Merges are sequential, Alec-approved.

| Track | Scope (one line) | Owned files | Executor model | Why |
|---|---|---|---|---|
| T1 backend-failures | Wire failure taxonomy: .vanished, cause mapping, native diagnostics, captureFailed event+banner, Copy Details detail, volume-echo reconcile, btLastUsed lock (perf P0), trim-range cache, "Speakers reconnected" honesty, DMR copy | NativeBackend, NativeCaptureCoordinator, ConnectionState, makeBackend seam; narrow: PopoverController note-kind, AppDelegate event case, LatencySettingModel+AudioSettings label | opus | Threading + event plumbing; well-anchored |
| T2 data-safety | Corrupt-store quarantine+surface, save rollback, try?-save reporting, devices snapshot (C8), persistRouting off-main (D4) | GroupController, all stores (GroupStore/AppRouteStore/RoutingStore/BTTrimStore/DeviceEQStore/ExcludedAppsStore/DeviceIconStore) | opus | Rollback semantics under concurrency |
| T3a popover-entrance | P0 reveal fix, empty/searching/permission states, occlusion gate, hidden-diff hoist, banners (re-theme, action, a11y role), Quit-toolbar move, BT-row affordance, dead blocked-path removal, telemetry line, save-group swallow, rebuild-vs-drag gate | AppSurfaceController, SurfaceSplashView, PopoverController, PopoverPanelViewController, SurfaceToolbar, both banner views | opus | Subtle AppKit window choreography |
| T3b popover-rows | Cursor rects+tooltips, AP1/+5 plain speech, diagnosis panel (✕/Escape/default/CopyDetails hide), FEED error shape, drag-flag fix, mouse-move via tracking areas, draw-mutation move, FeedPill IC observer, percent formatters in rows | DeviceRowView, AppRowView, GroupRowView, FeedPillView, ConnectionDiagnosisView | opus | Dense shared row class, a11y-sensitive |
| T4 license-money | About hide-seam/real values, key-prefix fix (pending worker check), in-session revalidation+Check Again, check-in disclosure, Remove confirm, sheet button refresh, key-field focus/normalization/substitutions, AX label cleanup, requiresApproval, excluded-apps hoist, credits/About-window fixes, validator timeout, https guard | AudioutSettingsUI/* , LicenseValidator, LicenseCheckIn (copy), AppSettings (https) | opus | Money path; correctness of state flow |
| T5 groups-window | Delete-copy P0 + alert hygiene, content-pane change gate (rename+rebuild), alreadyExisted dialog, creation-sheet errors+empty state, Edit menu, icon search, sidebar a11y+highlight, dup names, escape-rename, icon cache, menu retitle | AudioutWindowUI/*, DeviceIcon (cache); narrow: AppDelegate installMainMenu | opus | Many interacting AppKit fixes |
| T6 onboarding | Speaker Sync failure states + escape hatch, 14.2 floor, tone warning, broken-rows pressable, VO first-ask + autoPassed suffix + wait announce, focus restore, ✕ feedback, poll fixes, deep-link honesty, ListFormatter | AudioutOnboardingUI/*, SetupModel, SetupFlowModel, SystemSettingsOpener, Package.swift | opus | State-machine redesign |
| T7 shell | Reopen handler, autosaveName, mute-drain+failure glyph+AX desc, transient volume HUD, TouchBar scope+toggle+timer fix, wake re-arm, log gating, lazy stores, screen clamp, silent-item logging, copyright plist, status-menu items, Sparkle order, version-gate, translocation check | AudioutApp/* (non-menu AppDelegate regions), StatusItemController, StatusItemIcon, MenuBarStatus, TouchBarFullBar, VolumeKeyInterceptor, ControlPanelWindowController, make-app.sh; narrow: PopoverController.statusMasterVolume, one Settings toggle row | opus | New HUD UI + system integration |
| T8 perf-anim | FoldAnimator display link + reduce-motion gate, rail plan memoise, EQ static-layer split, appLevel coalescer, emitLevel atomic, EQ percent formatter | FoldAnimator, BusRailOverlayView, EQResponseCurveView, EQEditorView, NativeBackend (8600–8900 region only) | opus | Real-time + render discipline |
| T9 design-tokens | Mode-aware secondaryLabel, inkTertiary + consumer swaps, railDormant, warningText swap, accent observers (meter, armed dot), Subtle-ember re-tune, contrast-matrix test, NSFont duplicate replacement, rationale/doc corrections, dead-token deletion | Tokens.swift, LevelMeterView, RouteArmedDotView, docs/FIGMA-DESIGN-SYSTEM.md; one-line swaps in consumer files | opus | Measured contrast work + broad mechanical sweep |

FINAL models after model-critic pass (accepted 3 downgrades, rejected 3):
- opus: T1, T2, T3a (P0 window choreography has residual micro-decisions), T4 (money path), T6 (state-machine redesign, costliest regressions), T7 (new HUD UI + swizzle-adjacent), T8
- sonnet: T3b (anchored row-view edits), T5 (mechanical AppKit chains), T9 (computed hexes + rote swaps, matrix authored in the order)

Resolved inputs: naming = "Main Audio" (Alec); About = support@audiout.app + https://github.com/aa-hh/Audiout (repo verified PUBLIC); key prefix = AUDT confirmed from deployed worker (spec doc + fixtures are the stale side); server canonicalises keys fully (client normalization dropped).

Utility: license-server prefix check = sonnet (tool navigation, no reasoning depth).
Deferred to post-merge wrap-up: snapshot regeneration ×3, Main-Audio/Main-Out settlement, audiout:// URL handler, Group-N naming helper, theme-tile ring ask.
